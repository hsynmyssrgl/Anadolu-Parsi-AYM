# 33-Q / DEC-228 yerel yönetişimli OCR ve türetilmiş veri hattı tehdit modeli

- Durum: `PLANNED / LOCAL_IMPLEMENTATION_STARTED`
- Yerel uygulama: `PARTIAL_LOCAL_IMPLEMENTATION_COMPOSED / ACCEPTANCE_INCOMPLETE`
- Requirement PASS: `false`
- Yerel otomatik bileşen kanıtı: `20 dosya / 147 test PASS`
- Production runtime ve dış/manuel kanıt: `NOT_RUN`

| Tehdit | Zorunlu fail-closed kontrol | Mevcut gerçek / açık risk |
|---|---|---|
| Yetkisiz kişi belgeyi OCR işlemine verir | Kaynak okuma yetkisi, exact family/person/resource/sürüm, `archive.ocr/process/ocr_process` amacı ve merkezi PEP/UoW kararı aynı intentte doğrulanır. | Production adapter; source/settings/target receiptlerini tek async transaction içinde kurar, yeniden doğrular ve runtime-authority lease’e bağlar. Yerel entegrasyon PASS, gerçek cihaz/human acceptance açık. |
| Rıza gerektiren belge örtük veya süresiz işlenir | Kaynak/amaç/türev sınıfına bağlı açık ve süreli `sensitive_processing` rızası; iptal/expiry anında yeni iş kabulü kapatılır. | PEP amacı ile rıza amacı ayrı ve exact bağlanmıştır; OCR rızası `ai_process` izni değildir. Bu local composition testi privacy/human UAT değildir. |
| Belge şifrelenmeden veya hashlenmeden parser’a verilir | Önce şifreli arşiv kasasına atomik store, kaynak SHA-256/boyut doğrulaması, sonra bounded worker admission. | Production read chain archive vault `readBytes` + application use-case + main runtime adapter ile sınırlıdır; DataStore sealed payload allowlistinde değildir. Varsayılan malware provider yoksa admission fail-closed kalır. |
| Eski arşiv receipt’inde olmayan owner kimliği tahmin edilerek OCR yetkisi üretilir | OCR yalnız immutable create receipt’te exact actor-person `ownerPersonId` ve güncel kaynak sahipliği eşleşiyorsa açılır. Null owner için yalnız açık, item-bound ve güçlü kimlik doğrulamalı ownerless→actor re-attestation kabul edilir; sessiz/bulk backfill ve reassignment reddedilir. | Yeni import receipt’i actor-person bağlıdır. Legacy akış application/repository/SQLite/DataStore/IPC/UI düzeyinde 4 dosya/11 testle doğrulanır; migration 95 ledgerı immutable’dır. Bağımsız authentication attestationı ve gerçek cihaz/accessibility/privacy/security UAT yoktur; acceptance açık kalır. |
| Renderer farklı bir kişiyi hedefleyerek veya receipt/parola kanıtı uydurarak eski kaydı sahiplenir | Hedef kişi renderer’dan alınmaz; yalnız oturumdaki aktif `family_admin` actor-person kullanılır. Exact onay item kimliğine bağlıdır, parola/kod yalnız main strong-auth yoluna gider ve her sonuçta renderer state’i temizlenir. | IPC unknown-field/prototype/accessor negatifleri, safe-result projector, 6/dakika rate ve gerçek DataStore PEP/SQLite testi PASS’tir. Bulk backfill, sahip değişimi ve gizli receipt otoritesi yoktur. |
| Zararlı PDF veya parser açığı ana süreci ele geçirir | Zararlı dosya taraması, bounded format probe, PDF JavaScript/ek/harici referans reddi ve ayrı düşük yetkili worker. | Temel magic/active-token kontrolü tam malware kanıtı değildir; malware provider ve PDF rasterizer yoktur, PDF production yolu fail-closed unavailable kalır. |
| Zip bomb, dev görüntü veya çok sayfalı belge kaynak tüketimini bitirir | Şifreli envelope/dosya, dekompresyon, sayfa, piksel, CPU, RAM, wall-clock ve çıktı boyutu için hard cap; timeout/cancel fail-closed. | Production-bound child process; 16 MiB, 50 sayfa, 40M piksel, 250k metin, 1 MiB çıktı, concurrency=1 ve Job Object CPU/RAM/wall sınırlarına sahiptir. Tam adversarial belge matrisi ve düşük yetki doğrulaması açık. |
| Uzun OCR çalışırken kullanıcı iptal isteği işlem kilidi arkasında kalır | Worker çalışması uzun ömürlü yazma transaction’ı dışında yürütülür; cancel admission ayrı, eşzamanlı ve yetkili bir transaction ile aktif worker’a ulaşır. | `job_run_begin` kısa transactionı, detached main-only worker lease’i, FIFO kısa transaction kuyruğu ve terminal `job_run` finalizasyonu uygulanmıştır. Production executor probe’unda running Cancel runtime’a ulaşır, commit olur ve finalizasyon sonra tamamlanır; iki fazlı run/cancel local kanıtı `PASS`. Gerçek cihaz/human UAT `NOT_RUN`. |
| Renderer veya main process sınırsız parser çalıştırır | Renderer yalnız dar IPC komutu gönderir; main yetki/admission yapar; parser/OCR ayrı process içinde çalışır. | Dokuz dar IPC yöntemi ve main-only source-delete production source/testte vardır; parser bounded child process içindedir. Düşük yetkili sandbox doğrulanmamıştır. |
| Worker ağdan sessiz bulut OCR’a geçer | `ocr-worker` için ağ capability’si varsayılan deny; PPK-022 exact AST/build/runtime manifesti ve OS-enforced network sandbox. | Aggregate `windows-desktop` `ocr.process` ayrı worker kimliği değildir ve aynı aggregate kimlikte `network.access` vardır. `ocr-worker=[]`, sonuç alanında network/cloud false ve lowPrivilege false korunur; self-report OS-enforced ağ izolasyonu değildir. Residual açık. |
| AST/capability gate PASS sonucu worker sandbox acceptance sayılır | PPK-021 ve PPK-022 build/runtime yüzey ratchetleri ile process identity/sandbox/PEP kanıtları ayrı tutulur. | Current gate 442/685 ve 442/345 PASS; yine de `ocr-worker=[]`, lowPrivilege=false ve workerNetworkIsolation=false. Requirement PASS değildir. |
| Platform OCR yokken sessiz dış sağlayıcı kullanılır | Yalnız envanterli offline fallback; harici sağlayıcı ayrı açık rıza ve görünür seçim olmadan unavailable. | Windows adapter production facade’a bound; default malware provider yokken run fail-closed. Apple/offline providerlar `NOT_IMPLEMENTED`, harici sağlayıcı `NOT_CONFIGURED`. |
| Harici sağlayıcı gereğinden fazla belge alır | Gönderim önizlemesi, redaksiyon, minimum sayfa/bölge, exact provider/region/retention/deletion/contract ve tek kullanımlık karar. | Privacy contract ve canlı provider kanıtı `NOT_RUN`; provider availability/delivery garantisi yoktur. |
| Şifre çözülmüş belge veya OCR metni temp/log/cache’te kalır | Bounded memory/read stream, sıfırlama, şifreli result/index, içeriksiz log, crash/timeout cleanup ve düz metin dosya yasağı. | Owner-bound encrypted envelope vault, hardlink-safe publish/startup repair ve distinct maintenance PEP altında bounded orphan taraması testlidir; plaintext UAT/adversarial acceptance açık. |
| Retention süresi dolmuş OCR sonucu sealed vaultta kalır | Retention deadline read/admissionda uygulanır; expiry owner-bound purge adayına dönüşür ve exact job-delete PEP ile bounded/scheduled temizlenir. | Current sealed result için file-first purge ve scheduled maintenance authority testlidir; future index/cache/backup ve fiziksel erase ownerları açık olduğundan OCR-009 acceptance `false`. |
| Kaynak ile OCR sonucu karışır veya eski sonuç güncel görünür | Exact source resource/version/SHA, provider/model sürümü, language/page/coordinates/confidence ve derived SHA binding. | Migration 94, result validator, sealed vault ve exact current correction-chain/loser-branch cleanup yerel production composition testindedir; gerçek cihaz acceptance açık. |
| Görüntü OCR, gömülü PDF metni ve taranmış PDF aynı güven varsayımıyla işlenir | Üç ayrı format lane, ayrı parser allowlist ve ayrı negatif test matrisi. | PNG/JPEG image lane partialdır; PDF embedded/raster lanes `UNSUPPORTED_MEDIA/NOT_IMPLEMENTED` fail-closed. |
| OCR metni kaynak politikasından daha geniş erişim kazanır | PPK-016 exact recursive lineage; sensitivity, ownership, allowed actions/purposes, obligations, retention ve export daraltma. | PPK-016 local integration gate ve taze job/source PEP + ayrı rıza ile filtrelenen tam metin arama PASS; AI türev hattı ve 33-Q acceptance açık kalır. |
| İndeks veya snippet yetkisiz metin sızdırır | Her query/snippet merkezi PEP, sahiplik/policy filtresi ve alan maskelemesinden geçer; indeks doğrudan renderer’a verilmez. | Sabit boyutlu indeks şifreli owner-bound result zarfındadır; plaintext doğrulama ve sınırlı maskeli snippet testlidir. Legacy v1 sonuçlar rerun/correction olmadan aramada fail-closed; dış/human UAT `NOT_RUN`. |
| OCR sonucu resmî, sağlık veya finans gerçeği sayılır | Confidence ve kaynak görseli görünür; no-claim metni; kullanıcı düzeltmesi ve insan doğrulaması. | UI no-claim ve explicit result reveal içerir; gerçek erişilebilirlik/human UAT `NOT_RUN`, sertifikasyon iddiası yasaktır. |
| OCR metni otomatik AI girdisine dönüşür | Default deny; ayrı `ai_process` kararı, gerekirse ayrı rıza ve PPK-016 binding. | Otomatik AI feed yoktur; entegrasyon `NOT_IMPLEMENTED`. |
| Kullanıcı OCR’ı kapatamaz veya hatalı metni düzeltemez | Önizle, düzelt, yeniden çalıştır, dil seç, sil ve global/per-resource disable akışları. | Arşiv içi UI ve on dar bridge yöntemi kaynak/testte vardır; queued ve running iş iptal edilebilir, `cancel_requested` yeniden tetiklenemez. Real accessibility/human UAT yoktur. |
| Kaynak silinse de OCR/indeks/embedding/cache kalır | PPK-019 owner inventory, local-before-source-delete, atomik çoklu iş batch persistence, cache invalidation ve managed-backup pending zinciri. | Local OCR job metadata ownerı, sealed result ile aynı şifreli zarf içindeki current indeks purge, batch/current rows, repository-derived immutable item ledger ve authenticated restart auto-resume yerel production facade testindedir. Gelecekteki bağımsız index/embedding/cache ownerları ile managed-backup propagationı açıktır. |
| Permission veya süreli rıza iptal/expiry olsa da önceden üretilmiş OCR sonucu kalır | İptal olayı yeni admissionı durdurmakla kalmaz; exact source/owner lineage üzerinden mevcut sealed result, index, embedding ve cache için owner-bound karantina/purge planı üretir. | Current sealed result ve aynı şifreli zarf içindeki indeks için içeriksiz keşif, live denial revalidation, exact job-delete PEP, file-first idempotent purge, atomik mutation/audit/outbox ve authenticated bounded retry production testindedir. Gelecekteki bağımsız index/embedding/cache ve managed-backup ownerları açık olduğundan OCR-020 acceptance `false`. |
| Kaynak dosyası silindikten sonra OCR propagationından önce crash olur | Prepare → doğrulanmış source-file destroy → OCR purge/ledger → archive mark sırası korunur; source destroy başarısızsa silindi iddiası yazılmaz. File destroy sonrası ara durum durable kaydedilir ve açılışta/yeniden denemede otomatik, owner-bound ve idempotent devam ettirilir. | İçeriksiz immutable recovery ledgerı, canonical intent fingerprint revalidationı, normal PEP tekrar yürütümü, bounded scheduler ve gerçek temp SQLite restart testi source-file success penceresini kapatır. Bu yerel kanıt managed-backup/fiziksel erase veya gelecekteki türev ownerlarını kapsamaz. |
| UI crash-window retryını garanti gibi sunar | Domain ve safe IPC truth yüzeyi `sourceDeletionAutoResumeGuaranteed:true` değerini exact taşır; renderer bu alanı değiştiremez veya saklayamaz. | Değer yalnız aynı cihazdaki bound secure-destroy intentinin sonraki authenticated scheduler çevriminde devamını ifade eder. Requirement PASS, fiziksel erase ve tüm türev ownerlarının propagationı anlamına gelmez. |
| Quarantine fiziksel imha gibi sunulur | Yerel mantıksal silme, karantina, managed backup rewrite ve harici kanıt durumları ayrı gösterilir. | Fiziksel secure erase ve harici kopya imhası garanti edilmez. |
| Apple cihazına ham belge veya tam metin yetkisiz gider | Yalnız policy-authorized türetilmiş indeks/özet; ham belge/tam metin ayrı izin; recipient/device/epoch/version bağı. | Cross-device OCR sync `NOT_IMPLEMENTED/NOT_RUN`; teslim garantisi yoktur. |
| Ekran okuyucu metni yanlış sayfa/konumla sunar | Sayfa/koordinat/language bağları, kullanıcı düzeltmesi ve görsel orijinalin korunması. | Erişilebilirlik uygulaması ve gerçek cihaz UAT `NOT_RUN`. |
| Audit belge içeriğini veya path’i sızdırır | Yalnız talep/başlama/tamamlama/hata, provider/model ve kaynak/türev hash; payload, metin, snippet, ad ve path yasak. | Payload-free audit/outbox/mutation receipt source/testtedir; corrected text yalnız SHA-256 request fingerprintine girer. Retention/human review acceptance açık. |
| Mevcut temel paketler OCR kabulü gibi yorumlanır | Scope/inventory her temeli `REUSE_FOUNDATION_ONLY` olarak kaydeder; OCR provider-use-case-schema-UI-test zinciri ayrı zorunludur. | Bütün 33-Q requirementları registry’de `NOT_IMPLEMENTED`, evidence `false` kalır. |
| Starter verifier PASS sonucu requirement kapanışı sayılır | Artifact `governanceState=PLANNED`, `automatedRuntimeStatus=NOT_RUN` ve `countsAsRequirementPass=false` üretir. | Prepare/finalize/completion ve persistent receipt bu aşamada yoktur. |
| 33-P tamamlanmadan 33-Q aktive edilir | Plan, ledger, roadmap ve registry salt okunur doğrulanır; aktif adımın 33-P olması zorunludur. | 33-P dış/manuel kanıt nedeniyle `IN_PROGRESS`; 33-Q `PLANNED` kalır. |

## Zorunlu negatif test matrisi

Üretim uygulamasına geçildiğinde gate en az şu senaryoları gerçek source/test markerlarıyla ölçmelidir: yetkisiz kaynak ve foreign owner, stale policy/receipt/security epoch, revoked/expired consent, source SHA/size/version drift, symlink/path traversal, malicious PDF, PDF JavaScript/attachment/external reference, zip bomb, aşırı sıkıştırma, dev görüntü, sayfa/piksel/CPU/RAM/süre/çıktı kotası, bozuk font, worker crash/timeout/cancel/replay, aynı executor üzerinde gerçek production run+cancel yarışması, worker network denemesi, provider/version drift, düşük confidence, yanlış language/page/coordinates, plaintext temp/log/cache, index/snippet yetki sızıntısı, source-file destroy sonrası OCR propagation öncesi crash ve durable auto-resume/replay, kaynak silme/izin iptali sonrası orphan türev, yedek pending, automatic AI feed, cross-device over-sharing ve resmî/sağlık/finans overclaim.

Çok dil, Türkçe karakter, el yazısı, düşük kalite, ekran okuyucu ve kullanıcı düzeltmesi gerçek veri/cihaz UAT’si ayrıca `NOT_RUN` durumundan çıkmadan acceptance kapanmaz.

## Kalan kanıt ve iddia sınırı

Yerel domain/contract, migration 94-95, PEP/UoW, sealed vault/runtime, policy-filtered şifreli tam metin indeks ve maskeli snippet, Windows child-process adapter, DataStore facade, IPC, UI, legacy sahiplik yeniden doğrulaması ve sentetik zararlı-belge fail-closed matrisi `20 dosya / 147 test PASS` ile composition’a bağlıdır; bu yalnız `PARTIAL_LOCAL_IMPLEMENTATION_COMPOSED / ACCEPTANCE_INCOMPLETE` kanıtıdır. Aynı cihazdaki durable source-delete auto-resume, permission/rıza iptalindeki current sealed-result/index auto-reconcile, retention expiry purge ve distinct-maintenance-PEP orphan sweep gerçek temp SQLite/production adapter testlerinde vardır. Sentetik matris bozuk/poliglot görüntüleri, aktif/kaçışlı PDF işaretlerini, kotaları ve temiz olmayan tarayıcı kararlarını worker öncesinde reddeder; gerçek malware providerı veya PDF rasterizerı sayılmaz. Legacy v1 sonuçlar rerun/correction olmadan aramada fail-closed kalır. Gerçek cihazda kabul edilmiş Windows OCR, Apple Vision, denetlenmiş offline fallback, doğrulanmış düşük yetkili sandbox, tam gerçek-belge matrisi, cross-device ve dış provider kanıtı yoktur. Privacy, legal, security, accessibility ve human UAT `NOT_RUN` durumundadır.

Ham belge dışarı gönderilmez; sessiz cloud fallback yasaktır. Bununla birlikte üretim OCR hattı henüz çalışmadığı için bu ifade bir canlı sistem teslim kanıtı değildir. Remote delivery, provider availability, fiziksel secure erase, harici kopya imhası, resmî belge doğruluğu, sağlık/finans doğruluğu veya hukuki sertifikasyon garanti edilmez.

33-Q `PLANNED / LOCAL_IMPLEMENTATION_STARTED`, persistent receipt `NOT_RUN` ve `countsAsRequirementPass=false` kalır.
