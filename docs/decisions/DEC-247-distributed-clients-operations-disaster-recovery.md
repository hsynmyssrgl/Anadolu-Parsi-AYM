# DEC-247 — Dağıtık istemciler, bağlantı, operasyon ve felaket kurtarma

Durum: PLANNED / LOCAL_IMPLEMENTATION_STARTED

34-J keşfi yalnız doğrulanmış provider evidence taşıyan bir adres ipucu sayar; mDNS veya manuel IP/QR sonucu mTLS pairing yerine geçmez. Provider kimliği, production verification ve gerçek ağ kullanımı sonuçtan ayrı doğrulanır; exception halinde ağ kullanımı bilinmiyor tutulur. Remote varsayılan kapalıdır, inbound port gerektirmeyen outbound relay veya kullanıcı VPN'i kullanılabilir ve control plane yalnız exact beş içeriksiz şema varyantını kabul eder. Apple istemci yalnız read-only cache ve Core Service authorization evidence'ı ile planlanır; push teslimi garanti değildir.

Replica yedek değildir. Yedek kaydı doğrulayıcı provider, current cluster commit index ve cluster-state evidence hash'i, policy/key epoch, hedef kimliği, manifest/size ve previous-evidence hash zinciriyle tenant-scope kalıcı idempotency'ye bağlanır. Restore testi çağıran tarafından beyan edilemez; bu yerel runtime ve migration doğrulanmış restore kanıtı gelene kadar `restoreTested=false` zorlar. Rolling update planı doğrulanmış paket imzası, verifier tarafından ayrı doğrulanan N-1 uyumluluğu ve rollback artifacti, sağlıklı/eş epoch ve commit indexli eksiksiz cluster node envanteri ve follower-first/leader-last sıra olmadan üretilemez. Sentetik fault evidence ayrı append-only zincirde kalır ve gerçek Windows node kanıtı sayılmaz. Monotonic sayaç safe-integer ve strict artış; sync bütçesi bounded ve sessiz saat aralığı doğrulanmış olmak zorundadır.

Migration 114 checksum'u `ed39e408cbba4cc1b97d9e2e0a902f1b6573639c56a7585c3d44f48078e761fb` değeridir. Üç STRICT tablo, tenant-scope kimlik/idempotency, immutable update/delete triggerları, non-zero kanıtlar, backup/fault sıra-zaman ve previous-hash zinciri, cluster/epoch/commit bağı ve sağlıksız/uyuşmayan node listesini reddeden exact leader-last update envanteri uygular. Append-only kanıt tabloları için ürün retention/archival politikası henüz kararlaştırılmamıştır. Buna karşın üretim runtime composition'ı ve providerlar yoktur. Migration 114 ve sentetik testler gerçek mDNS, relay/VPN, Apple uygulaması, farklı cihaz restore, rolling update, break-glass recovery veya Windows fault matrix değildir. Bu kanıtlar `NOT_RUN`, `countsAsRequirementPass=false` kalır.
