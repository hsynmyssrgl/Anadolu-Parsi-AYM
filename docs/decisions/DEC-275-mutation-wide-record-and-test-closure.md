# DEC-275 — En küçük değişiklikte tüm kayıt ve test kapanışı

- Tarih: 24.08.2026
- Durum: ACTIVE
- Kural: PR-240
- Görünür sürüm: Bronze 22.08.2026.50

## Karar

En küçük kod, yapılandırma, belge, test veya kanıt üreticisi değişikliği dahi yalnız değişen dosyada bırakılamaz. Etkilenen ana kaynak, Bronze/Silver/Gold kanal kaynakları, kanonik kural ve kullanıcı karar sicilleri, aktif ve ticari belgeler, iş listesi, kapsam, envanter, ratchet, manifest, indeks, güncel ana DOCX/PDF ve kanıt sözleşmeleri aynı mutasyon zincirinde güncellenir. Kanonik etki değerlendirmesi her bağımlı sınıf için `UPDATED` veya somut gerekçeli `NOT_AFFECTED` sonucu üretmeden kapanış yapılamaz.

Byte-exact tarihsel ve değişmez bir kayıt, güncel kaynak değişti diye yeniden yazılacak `dependentRecord` sayılamaz; doğrudan değiştirilmesi bütün ilgili sözleşme ve değişmezlik kapılarını tetiklemeye devam eder. Güncel ratchet, belge ve resmî üretici makbuzları ise bağımlı kayıt olarak Git teslimine girer. Böylece aynı dosyayı hem değiştirme hem de byte-exact koruma zorunluluğu doğuran uygulanamaz kapanış döngülerine izin verilmez.

Her değişiklikte hedefli test, filtresiz tam regresyon, typecheck, değişen MJS/PowerShell sözdizimi ve kaynak bütünlüğü aynı exact commit için PASS olmalıdır. Kullanıcı arayüzüne dokunan değişiklikler dört modül, yirmi iki rota, ana/alt menüler, görünür uygun kontroller, açılan durumlar, erişilebilirlik ve görsel bütünlük kapsamının tamamında kurulu uygulama UAT'ına girer. Yalnız buton saymak yeterli değildir; sonuç, hata ve taşma/örtüşme oracle'ları doğrulanır.

Gerçek test hataları boş `wip(rejected)` checkpoint commit ile tarihçeye yazılır. Tüm kaynak ve kayıt kapanışı bitmeden ara installer üretilemez. Paket yalnız temiz exact committen ve ana kaynak ile ilgili kanal kaynak eşitliği doğrulandıktan sonra oluşturulabilir. Eksik, eski veya gerekçesiz kayıt fail-closed engeldir; waiver yoktur.

FAIL makbuzu; ham çıktı, stack, mutlak dış yol veya sır değeri taşımadan repo-relative test dosyasını ve güvenli test adını kaydeder. Suite yüklenemiyorsa bu durum ayrı bir `SUITE_IMPORT` kimliğiyle görünür olur. Böylece hata ayrıntısı için aynı uzun test turunun ikinci kez çalıştırılması gerekmez; sayaçlar test dosyası, Vitest suite ve assertion anlamları karıştırılmadan raporlanır.

Git tarafından izlenmeyen veya ignore edilen eski bir üretici çıktısı testin kanonik girdisi olamaz. Salt-okunur test, canlı üreticinin yapılandırılmış stdout çıktısını ya da benzersiz geçici hedefini kullanır ve sürüm beklentisini aktif build metasına bağlar. Kanal kanıtında salt-okunur doğrulayıcı yalnız exact Git kökü olduğu doğrulanan `app` veya Bronze/Silver/Gold çalışma kopyasında çalışabilir; çıktı yazan mod ana `app` köküyle sınırlı kalır. Derlenmiş test önkoşulu gerekiyorsa bu hazırlık açık, tekrarlanabilir ve aynı çalışma kopyasına bağlı olmalıdır; eski `.tmp` veya `dist` kalıntısı PASS üretemez.

Canlı güvenlik kapısı tarihsel paket kapsamındaki kopya özete bağlanamaz. PPK-015 gibi güncel ratchet kararları tek kanonik `currentBoundary` kaydından okunur ve canlı rapor; kaynak bölgesi/dosya/özet, kötü amaçlı öz-test, bulgu, yetkili adapter/purpose, yalnız-yerel taşıma ve doğrudan istisna alanlarının tamamında karşılaştırılır. Eski kapsam ve envanter değerleri yalnız paket-zamanı snapshot iç tutarlılığı için korunur; güncel PASS üretmez. Güncel belge kanonik ratchet ile eşzamanlı yenilenir ve değiştirilmiş sayı ya da özet negatif testte reddedilir.

Bağımlı kayıt fiziksel diff taşımıyorsa sahte/no-op değişiklik yapılamaz. Etki değerlendirmesi bu kaydı yalnız baseline diff envanterinde bulunmadığı, güncel SHA-256 değeri okunduğu, `DEPENDENT_RECORD_BASELINE_IDENTITY_UNCHANGED` gerekçesi ve kanıt yolu verildiği zaman `NOT_AFFECTED_WITH_BASELINE_IDENTITY` olarak kabul eder. Değişen kayıt exact `UPDATED` ve kendi yoluyla; değişmeyen kayıt ise assessment, impact-analysis ve güncel dosya readback SHA bağlarının üçü aynıysa kapanır. Eksik kayıt, değiştirilmiş hash veya assessment/analysis ayrışması fail-closed reddedilir.

Bu kararın uygulanmasında gerçek test hataları `5d56bcac`, `a7d50f72`, `925d25d1`, `e42f9b58`, `4894345b`, `88fb84f8`, `447eb103`, `90b0fb0f`, `5490c2b4`, `284545ff` ve `41c16c13` reddedilmiş checkpointleriyle korunmuştur; bunlar PASS veya sürüm kabulü değildir. `284545ff`, resmî ana build ledger üreticisinin çift satır sonu üretmesini `git diff --check` kapısında kaydeder; düzeltme yalnız çıktıya değil kanonik üreticiye uygulanır. `41c16c13`, Bronze exact hedefli regresyonda 596 test geçerken `governed-source-root` ana-kaynak testinin kanal çalışma dizinini ana kaynak sanmasını kaydeder; düzeltme explicit ana kaynak kabulü ile mevcut exact checkout kabulünü ayrı testlere bağlar.

Tam regresyonun exact Bronze kanal kosusunda tum Vitest ve ek komutlar tamamlandiktan sonra alti izlenen PPK-015/021/022 makbuzunun degismesi temiz kaynak kapisini fail-closed dusurmustur. Bu gercek hata `65db62ad` bos `wip(rejected)` checkpointiyle korunur; PASS sayilmaz. Kok neden, `34-B`–`34-F` runtime dogrulayicilarinin dis `--no-write` bayragini npm ile cagirdigi alt PPK ve veri makbuzu ureticilerine aktarmamasi ile PPK-022 runtime ureticisinin bayragi hic uygulamamasidir. Alt komut aktarimi ve kanal-kok kabul siniri duzeltildikten sonra gercek `34-B --no-write` zinciri 13/13 PASS vermis ve ilgili alti makbuzun SHA-256 degerleri 6/6 degismemistir. Onceki hata byteslari geri donulebilir yerel Git stash `9ba3a77b5e7ae0f456ef98feb1fd2240c00c000f` icinde saklanir; yeni exact hedefli/tam regresyon ve kaynak butunlugu tamamlanmadan paket yoktur.

Son salt-okunur kaynak denetimi, `34-G`–`34-K` remaining-package runtime zincirinin migration, PPK-021 ve PPK-022 alt doğrulayıcılarına dış `--no-write` bayrağını iletmediğini göstermiştir. Bu ek gerçek kusur `90b5ad40` boş `wip(rejected)` checkpointiyle korunur. Ortak `governedNodeScript` aktarımı ve kaynak regresyon testi eklendikten sonra odaklı 1 dosya/6 test, gerçek `34-G runtime --no-write` 37/37 PASS vermiş; migration, PPK-021 ve PPK-022 makbuzlarının üçü de SHA-256 olarak değişmeden kalmıştır. Bu odaklı sonuç da yeni exact filtresiz tam regresyonun yerine geçmez.

Bu düzeltmelerden sonraki exact Bronze filtresiz tam regresyonu bütün Vitest testlerini ve 47 ek komutu tamamlamış, ancak çalışma sonu mutasyon koruması yeni oluşan `artifacts/validation/desktop-core-service-startup-runtime.json` makbuzu nedeniyle fail-closed durmuştur. Gerçek hata `4c6652e0` boş `wip(rejected)` checkpointiyle korunur. Kesin zincir `34-D runtime --no-write` → `PPK-022 runtime --no-write` → masaüstü başlangıç sarmalayıcısı → son çalışma doğrulayıcısıdır; sarmalayıcı bayrağı aktarmadığı için son üretici makbuz yazmıştır. PPK-022 çağrısı, sarmalayıcı ve masaüstü başlangıç çalışma/sözleşme üreticileri kalıcı `--no-write` desteği ve kaynak regresyon testiyle kapatılmıştır. Odaklı regresyon 1 dosya/6 test, gerçek çalışma 51/51, sözleşme 41/41 ve uçtan uca PPK-022 matrisi 24/24 PASS vermiş; 1.571 doğrulama dosyasının sıfırı eklenmiş veya değişmiştir. Bu sonuç yeni temiz exact committe hedefli ve filtresiz tam regresyon zorunluluğunu kaldırmaz.

Ana belge render zincirindeki iki gerçek altyapı hatası da gizlenmemiştir: `47af84bd` LibreOffice yürütülebilirinin, `b34e951b` ise Poppler `pdfinfo` yürütülebilirinin PATH üzerinde çözülememesini boş `wip(rejected)` checkpoint olarak kaydeder. Araçların doğrulanmış mutlak dizinleri PATH'e eklenince 28/28 sayfa başarıyla render edilmiş ve her sayfa özgün çözünürlükte taşma, örtüşme, kesilme ve bozuk karakter açısından görsel denetimden geçmiştir. İçerik denetiminde Build 164 ve Build 169 kararlarının aynı `ADR-039` kimliğini kullanması ayrı gerçek kusur olarak saptanıp `10282bf4` ile reddedilmiş checkpoint'e alınmıştır. Build 164 kaynak bütçesi kararı `ADR-039` kalır; daha yeni Build 169 güçlü yeniden doğrulama kararı benzersiz `ADR-107` kimliğine taşınır. Kayıt yenilemesinden sonra nihai DOCX/PDF yeniden üretilip bütün sayfalar tekrar render edilmeden belge kapanışı PASS sayılamaz.

Güncellenen ana DOCX'in bundled belge aracıyla ilk görsel QA çağrısında LibreOffice çalıştırılabilirinin PATH üzerinde yeniden çözülememesi `6ec632c8` boş `wip(rejected)` checkpointiyle ayrıca korunur. Aynı DOCX doğrulanmış mutlak `C:\Program Files\LibreOffice\program` ve bundled Poppler diziniyle yeniden render edilerek 28/28 sayfa üretmiştir. Önceki onaylı renderla 2–6 ve 27–28. sayfalar SHA-256 özdeş kalmış; kaynak HEAD ve yeni kapanış maddesi nedeniyle değişen 1 ve 7–26. sayfalar taşma, örtüşme, kesilme ve bozuk karakter açısından görsel olarak yeniden incelenip PASS verilmiştir.

Yeni kimlik kapısının ilk gerçek koşusu, tarihsel ADR/DEC başlıklarında kabul edilmiş `:`, `-`, `—` ayraçlarını kimlik uyuşmazlığı sanarak fail-closed durmuştur; sonuç `9f2aaf86` boş `wip(rejected)` checkpointiyle korunur. Salt-okunur envanter taraması başka yinelenen kimlik bulunmadığını göstermiştir. Kapı biçim ayraçlarını yeniden yazmak yerine dosya adındaki `ADR-xxx`/`DEC-xxx` kimliğinin başlık başlangıcıyla exact eşliğini, PR/DEC/ADR/IS/TICARI kimliklerinin kendi ad alanlarında tekilliğini ve ticari `sonKayit` bağını doğrular. Düzeltilmiş kural kapısı ile odaklı 2 dosya/12 test PASS vermiştir.

Son exact kapanışta etki değerlendirmesi ve bağımlılık analizi `3b06fe51` için 577 değişen yol ile 94 hedefli test dosyasını PASS hesaplamış, ancak hedefli test üreticisi zorunlu açık `-- -- <repo-içi-testler>` argümanları verilmeden çağrıldığı için test başlamadan fail-closed durmuştur. Bu gerçek komut sözleşmesi ihlali `4c8b6b7d` boş `wip(rejected)` checkpointiyle korunur. Test kodu ve hesaplanan kapsam değiştirilmez; yeni koşu analizden türetilen 94 dosyayı exact sırayla açıkça aktarır. Bu ret hedefli veya tam regresyon PASS değildir.

Kanonik proje indeksi `--git-index` ile üretildikten sonra doğrulayıcının varsayılan canlı çalışma ağacı modunda çağrılması, ignored tarihsel artefaktların doğal kapsam farkında fail-closed durmuştur; `47f441e1` bu yanlış-mod çağrıyı boş `wip(rejected)` checkpoint olarak korur. Üreticiyle aynı salt-okunur `--git-index --no-report` modu 13.146 kontrol, 4.407 dosya ve 2.143 belge PASS; kaynak manifesti 4.868/4.868 dosya ve 4.869 SHA PASS vermiştir. Yanlış-mod ret bu doğru-mod PASS ile silinmez veya PASS sayılmaz.

`cb20d2e5` exact Bronze tam regresyonunda 398 Vitest dosyasının ve 2.469 testin tamamı PASS vermiş, fakat 16 ek runtime komutu Bronze çalışma kopyasında derlenmiş workspace girişleri bulunmadığı için FAIL olmuştur; sonuç `703be65a` boş `wip(rejected)` checkpointiyle korunur. `0a118f5f`, aynı komutların tanısında resmî npm çalıştırıcısının `npm_execpath` ortamı taşınmadan yapılan yanlış yeniden çağrıyı; `7fb288cd` ise `build:packages` sonrasında 34-I DPAPI turunun eksik `apps/core-service/dist/main.js` nedeniyle durmasını ayrıca korur. Resmî paket, core-service ve desktop build önkoşulları aynı Bronze kökünde üretildikten sonra önceki 16 ek runtime komutunun tamamı PASS vermiştir. Bu odaklı yeniden kontrol yeni exact commit tam regresyon PASS makbuzu değildir.

Runtime yeniden kontrolü sonunda izlenen migration manifestinde yalnız `generatedAt` değişikliği kalması, 33-Y, 33-Z ve 34-A runtime sarmalayıcılarının dış `--no-write` bayrağını migration, data-store smoke ve politika alt süreçlerine yaymadığını göstermiştir; gerçek test yan etkisi `ddb1abff` boş `wip(rejected)` checkpointiyle korunur. Üç sarmalayıcı ortak `governedNodeScript` aktarımına bağlanmış ve kaynak regresyon testi eklenmiştir. Odaklı 1 dosya/6 test ile 33-Y 11/11, 33-Z 11/11 ve 34-A 11/11 gerçek runtime kontrolleri PASS vermiş; migration manifestinin SHA-256 değeri her üç çalışmadan önce ve sonra byte-exact aynı kalmıştır.

Güncel master DOCX'in LibreOffice ile PDF'e dönüşümü başarıyla tamamlanmış, fakat ilk PNG render denemesinde bundled Poppler `pdfinfo` dizini PATH'e eklenmediği için fail-closed durmuştur; `dd675310` boş `wip(rejected)` checkpointi bu altyapı hatasını korur. Exact bundled Poppler ve LibreOffice dizinleriyle yeniden deneme 28/28 PNG sayfa üretmiştir. Önceki onaylı renderla 10–25. sayfalar byte-exact aynı kalmış; kaynak HEAD ve yeni kapanış maddeleri nedeniyle değişen 1–9 ile 26–28. sayfalar özgün çözünürlükte taşma, örtüşme, kesilme ve bozuk karakter açısından yeniden incelenip PASS verilmiştir.

Release kaynak bütünlüğü doğrulayıcısının ana `app` çalışma ağacında çağrılması beklenen exact Bronze kök şartını karşılamadığı için fail-closed durmuştur; `99ad48dd` boş `wip(rejected)` checkpointi bu yanlış worktree çağrısını korur. Bu kayıt ürün kaynak bütünlüğü hatası değildir ve PASS yerine geçmez; aynı doğrulayıcı exact Bronze çalışma ağacında yeniden çalıştırılıp başarılı olmadan paket üretilemez.

`0669cb38` kaynak commitinden yeniden üretilen final master DOCX, exact bundled LibreOffice ve Poppler yollarıyla 28/28 PNG sayfa üretmiştir. Önceki onaylı renderla 2–6. sayfalar byte-exact aynı kalmış; içerik ve kaynak HEAD değişimi nedeniyle farklılaşan 1 ile 7–28. sayfalar özgün çözünürlükte tek tek incelenmiş, taşma, örtüşme, kesilme veya bozuk karakter görülmemiştir.

`80cf2a39` exact Bronze kaynağında hedefli 94 dosya/598 test ve filtresiz 398 dosya/2.469 test PASS vermiş, ancak ek komut matrisi sonunda izlenen migration manifestinin yalnız `generatedAt` alanı değiştiği için temiz çalışma ağacı kapısı fail-closed durmuştur. Gerçek test yan etkisi `c7a3c130` boş `wip(rejected)` checkpointiyle korunur. Kök neden, `33-R` runtime sarmalayıcısının dış `--no-write` bayrağını migration, data-store smoke, PPK-021 ve PPK-022 alt doğrulayıcılarına aktarmamasıdır. Dört alt çağrı ortak `governedNodeScript` aktarımına bağlanmış; odaklı kaynak regresyonu 1 dosya/6 test ve gerçek `33-R --no-write` matrisi 11/11, 8 dosya/30 test PASS vermiş, migration manifest SHA-256 değeri byte-exact değişmeden kalmıştır. Bu odaklı PASS yeni temiz exact commit hedefli/tam regresyon ve exact Bronze kaynak bütünlüğü zorunluluğunu kaldırmaz.

`bfb6db9f` exact Bronze kaynağında hedefli 94 dosya/598 test ve filtresiz 398 dosya/2.469 test PASS vermiş, ancak tam regresyon üreticisinin resmî npm girişi yerine doğrudan `node` ile çağrılması `npm_execpath` bağını kaldırdığı için 171 ek komuttan `34-B`, `34-C`, `34-D` ve `34-F` runtime sarmalayıcıları ortak npm alt çağrılarında FAIL olmuştur. Bu operatör çağrı hatası `51316ac3` boş `wip(rejected)` checkpointiyle korunur; kaynak veya test PASS sayılmaz. Aynı `34-B --no-write` zinciri kanonik npm scriptiyle 13/13, 5 dosya/30 test PASS vermiştir. Nihai tam regresyon yalnız resmî `npm run verify:mutation-full-regression:evidence` girişiyle yeniden çalıştırılır.

Resmî npm girişiyle `c02744cd` exact Bronze tam regresyonu 398 dosya/2.469 test ve 171/171 ek komut PASS vermiştir. Bunu izleyen exact Bronze kaynak bütünlüğü manifestte 4.868 dosya, canlı ağaçta 4.186 dosya ve 4.869 SHA girdisi ölçmüş; 683 manifest yolu eksik ve linked-worktree `.git` yönetim dosyası manifest dışı olduğu için 684 bulguyla FAIL olmuştur. Gerçek sonuç `7d67fcff` boş `wip(rejected)` checkpointiyle korunur ve tam regresyon PASS'i kaynak bütünlüğü PASS'i olarak yorumlanmaz.

Kök neden, Git tarafından izlenmeyen fakat kaynak teslim manifestine bağlı yönetişim/checkpoint payloadının ayrı kanal çalışma dizinlerine fiziksel olarak eşitlenmemesi ve kaynak toplayıcının `.git` adını yalnız klasör olduğunda dışlamasıdır. Kanal worktree kurulumu bundan sonra manifestteki Git-dışı girdileri yalnız kanonik göreli yol, normal dosya, byte sayısı ve SHA-256 kimliği doğrulanınca her ayrı Bronze/Silver/Gold dizinine atomik olarak yazar ve canlı readback yapar; Git'te izlenen dosyalara dokunmaz, stale fazlalığı sessizce silmez. Kaynak manifesti linked-worktree `.git` dosyasını ve normal checkout `.git` klasörünü aynı biçimde dışlar. Odaklı 2 dosya/9 test PASS vermiştir; yeni exact commit hedefli/tam regresyonu ve exact Bronze kaynak bütünlüğü zorunludur.

`96b9faac` kaynaklı güncel ana DOCX'in ilk PNG render çağrısında yanlış bundled bağımlılık PATH'i kullanıldığı için PDF dönüşümü tamamlanmasına rağmen Poppler `pdfinfo` çözülememiş ve görsel QA fail-closed durmuştur. Gerçek altyapı hatası `e5787764` boş `wip(rejected)` checkpointiyle korunur. Doğru `C:\Users\Husey\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\poppler\Library\bin` ve `C:\Program Files\LibreOffice\program` yollarıyla yeniden deneme 28/28 PNG sayfa üretmiştir. Bütün sayfalar dört temas sayfasında; metin yoğun 6–7 ve tablo yoğun 27–28. sayfalar ayrıca özgün çözünürlükte incelenmiş, taşma, örtüşme, kesilme, font veya bozuk karakter kusuru görülmemiştir. Bu PASS yanlış-yol ret kaydını silmez ve yeni exact hedefli/tam regresyon, kaynak bütünlüğü ile kurulu tam UI UAT zorunluluğunu kaldırmaz.

Kapanış sicilleri `7f866e69` kaynak commitinde birleştirildikten sonra ana DOCX/PDF yeniden üretilmiş ve doğru native Poppler/LibreOffice araç zinciri 29/29 PNG sayfa vermiştir. Bütün sayfalar beş temas sayfasında; metin yoğun 6–7 ile kural ve tarihsel envanter tablolarını taşıyan 26–29. sayfalar ayrıca özgün çözünürlükte denetlenmiş, görsel kusur bulunmamıştır. Bu son QA, `e5787764` ret kaydını koruyarak belge kapanışını güncel kaynakta doğrular.

Exact `d421c299` Bronze kaynakta hedefli 95 dosya/600 test, filtresiz 399 dosya/2.471 test, 172/172 ek komut ve 4.869/4.869 kaynak dosyası PASS vermiştir. Paket öncesi governed preflight'ın ilk çağrısı, eski installer temizliği sonrasında güncel kullanıcıya görünür sürüm ve retention makbuzları henüz yeniden üretilmediği için fail-closed durmuş ve `607a9a53` ile korunmuştur. İki kapı yeniden üretildikten sonraki ikinci çağrı, çalışma planındaki tamamlanmış adımların istediği 1.428 kanıt yolundan exact Bronze kanalında 803 Git-dışı checkpoint payload dosyası bulunmadığı için durmuş ve `8b2b5ccc` ile korunmuştur. Ana kaynakta 1.428/1.428 yolun mevcut olduğu doğrulanmıştır.

Kök neden, önceki kanal payload düzeltmesinin yalnız `manifest.json` kapsamını taşıması; `config/work-segmentation-plan.json` içindeki tamamlanmış adımların `localEvidence` ve persistent receipt yollarını bağımsız preflight girdisi olarak kapsamamasıdır. Worktree kurulumu artık bu ikinci kümeyi tracked ve zaten manifest-bound yolları dışlayarak; kanonik göreli yol, normal dosya, SHA-256, atomik yazım ve canlı readback ile her kanala eşitler. Devam eden adımın mutasyona açık kanıtları taşınmaz. Seçici 809 non-tracked/non-manifest yolu belirlemiş; odaklı sözleşme testi 1 dosya/9 test ve ticari temel 1.234 kontrol/87 dosya/60 iş/240 kural PASS vermiştir. Üç kanal eşitlemesi ve aynı exact final commit preflight PASS olmadan paketleme başlamaz.

Bu kalıcı düzeltmeyi içeren `65ffd15d` kaynak commitinden güncellenen master DOCX, doğru native LibreOffice ve Poppler araç zinciriyle 29/29 PNG sayfa üretmiştir. Sayfaların tamamı özgün çözünürlükte tek tek incelenmiş; taşma, örtüşme, kesilme, font veya bozuk karakter kusuru görülmemiştir. Bu belge kapanışı yeni exact kanal eşitliği, hedefli/tam regresyon, kaynak bütünlüğü, preflight ve kurulu uygulama UAT zorunluluklarını kaldırmaz.

`0f0a4653` exact Bronze etki değerlendirmesi ve bağımlılık analizi 95 hedefli test dosyasını hesaplamıştır. Hedefli turda 600 test PASS iken `operation-rule-check-policy.test.ts`, aktif ürün yaşam döngüsünden ayrı PR-240 teslim kapanışının güncel mutasyon `preflightStatus` alanında tarihsel `607a9a53/8b2b5ccc` FAIL metnini taşımasını reddetmiştir. Gerçek hata `50f4d9e5` boş `wip(rejected)` checkpointiyle korunur. Tarihsel preflight retleri `latestRejectedCheckpoint`, payload/QA ve karar kayıtlarında kalır; güncel mutasyon alanı yalnız yeni exact preflight gerçekten çalışana kadar `NOT_RUN_CURRENT_MUTATION` olur. Kalıcı hidrasyon sonrasında Bronze, Silver ve Gold'un her birinde tamamlanmış adımların 1.428/1.428 zorunlu yolu mevcut, eksik 0'dır. Yeni exact hedefli ve tam regresyon bu düzeltme commitinde yeniden çalıştırılmadan paketleme başlamaz.

`cc01412c` sertifikalı exact Bronze kaynağında hedefli tur 95 dosya/601 test, filtresiz tam tur 399 dosya/2.472 test PASS vermiştir. Preflight sertifika commit'inin güncellediği sekiz izlenen indeks/kanıt dosyasının yeni hash'leri `manifest.json` ve `SHA256SUMS.txt` içine alınmadığı için final kaynak bütünlüğü 4.869 manifest/4.869 kaynak/4.870 SHA sayılarıyla FAIL olmuş ve `6a0840d4` boş `wip(rejected)` checkpoint'iyle korunmuştur. Düzeltme; aktif kayıtlar ve indeksler tamamlandıktan sonra manifest ile SHA listesini son üretici olarak yeniden oluşturur. Bu düzeltme commit'inde exact hedefli, tam regresyon, kaynak bütünlüğü ve read-only pre/postflight yeniden PASS olmadan paketleme başlayamaz.

`96377fb9` exact kaynakta hedefli 95 dosya/601 test, filtresiz 399 dosya/2.472 test, kaynak bütünlüğü 4.869 manifest/4.869 kaynak/4.870 SHA, governed preflight/postflight ve ana/Bronze/Silver/Gold commit eşitliği PASS vermiştir. Temiz Windows paketleme kapısı daha sonra kanonik `LATEST` makbuzunun dış D: koruması tamamlanmış `PASS` durumunu, ağaç adına bağlı değişmez kaydın ise ilk yerel `PENDING` durumunu taşıdığını ve byte-exact eşit olmadığını reddetmiştir. Gerçek paket hatası `b87ebe2e` boş `wip(rejected)` checkpoint'iyle korunur; installer üretilmemiştir.

Kalıcı düzeltmede ilk yerel `PENDING` koruma kaydı tarihsel ve değişmez kalır. Dış USB doğrulaması bitince dış makbuzun SHA-256 kimliğine bağlı ikinci bir değişmez `PASS` koruma kaydı ve sidecar üretilir, ikisi D: hedefinde yeniden okunur ve kanonik `LATEST` yalnız bu ikinci kaydın byte'larıyla eşleşir. Dış envanter ilk koruma, receipt/readback çiftleri ve final koruma çiftiyle 11 dosyadır. Hedefli güvenlik sözleşmesi 2 dosya/4 test PASS vermiştir; yeni exact committe bütün kayıt, hedefli/tam regresyon, kaynak bütünlüğü, pre/postflight, yerel+dış yedek, paket ve kurulu tam UI UAT yeniden yapılmadan teslim kapanmaz.

Genişletilmiş hedefli retryda 28 test PASS iken `operation-rule-check-policy.test.ts`, paket durumunu fail-closed anlamı yerine yalnız eski sabit `BLOCKED_UNTIL_CLEAN_EXACT_COMMIT` metnine bağladığı için güncel gerçek `FAIL_B87EBE2E_...` ayrıntısını reddetmiştir. Bu sözleşme drifti `a7b0f3c3` boş `wip(rejected)` checkpoint'iyle korunur. Sözleşme paket durumunu `BLOCKED_` veya `FAIL_` önekiyle sınırlayarak PASS/READY durumunu dışlar; 4 dosya/29 test retry PASS vermiştir.

Bu kayıtları taşıyan master DOCX kanonik LibreOffice/Poppler zincirinde 29 PNG sayfa üretmiştir. Sayfa 2–29 önceki tam onaylı renderla byte-exact aynıdır; yalnız kaynak HEAD satırı değişen 1. sayfa özgün çözünürlükte taşma, örtüşme, kesilme, font ve bozuk karakter kusuru olmadan PASS vermiştir.

`1745095c` düzeltme commit'i sonrasında kanal eşitleyicinin var olan Bronze dalı authoritative HEAD'e ilerletilmeden varsayılan doğrulama davranışıyla çağrılması commit farkını hiçbir kanal dosyasına yazmadan fail-closed reddetmiş ve `d5540157` ile korunmuştur. Bu araç mevcut kanal dallarını kendi başına taşımaz. Doğru mutasyon sırası Bronze/Silver/Gold worktree dallarını exact authoritative HEAD'e yalnız `git merge --ff-only` ile ilerletmek; ardından eşitleyiciyi manifest ve tamamlanmış checkpoint payloadını hydrate ederek branch/commit/ortak Git nesne deposu/temiz ağaç eşitliğini doğrulamak için çalıştırmaktır. Reset veya geçmişi yeniden yazma yoktur.

`b1d237db` son kaynak değişikliğinden sonra eski kalan governed-preflight sertifikasını, `c9f2eebe` eski mutation impact sourceCommit bağını ve `5179a170` hedefli test üreticisinin zorunlu `--` sonrası 95 dosya listesinin verilmemesini gerçek fail-closed checkpointler olarak korur. Düzeltilmiş `5179a170` exact turunda hedefli 95 dosya/601 test ve filtresiz 399 dosya/2.472 test PASS vermiş; 172 ek komuttan 34-B/C/D/F dış runtime'ları Bronze çalışma ağacında bulunmayan `.tmp/npm-10.9.2/package/bin/npm-cli.js` yoluna düştüğü için toplam tur FAIL olmuş ve `f8946730` ile korunmuştur. `1cb33525` tanı checkpointi boundary/contract adımlarının PASS, npm kullanan bütün alt adımların aynı anda FAIL olduğunu kaydeder. Kalıcı çözüm 34-B–34-F runtime'larında npm CLI'yi önce aktif npm ortamından, sonra aktif Node kurulumundan, son olarak kontrollü geçici fallback'ten varlık doğrulamasıyla seçer ve aday yoksa açıkça reddeder. Odaklı sözleşme 1 dosya/6 test ve `npm_execpath` olmadan gerçek 34-B 13/13, 34-C 14/14, 34-D 14/14, 34-E 15/15, 34-F 15/15 PASS vermiştir. Bu PASS'ler yeni temiz exact hedefli/tam regresyon, kaynak bütünlüğü, pre/postflight, yedek, paket ve kurulu tam UI UAT zorunluluğunu kaldırmaz.

Bu karar `DEC-270/PR-235` exact-commit etki ve test zincirini kaldırmaz; kapsamını bütün kayıt sınıfları ve tüm kullanıcı etkileşimi/görsel bütünlük yüzeyleri için açıkça güçlendirir.

Bu kapanışın son belge QA turunda `c5879c9a`, LibreOffice yürütülebilirinin PATH üzerinde bulunmamasını; `194a8281`, DOCX-PDF dönüşümü PASS sonrasında Poppler `pdfinfo` yürütülebilirinin bulunmamasını; `62d0b1d8` ise yalnız temas sayfası yardımcı komutundaki sözdizimi hatasını boş `wip(rejected)` checkpointleri olarak korur. Doğrulanmış `C:\Program Files\LibreOffice\program` ve native Poppler yollarıyla kanonik retry 29/29 sayfa üretmiştir. Bütün sayfalar üç temas sayfasında, metin yoğun 6–8 ve kural/envanter yoğun 27–29. sayfalar ayrıca özgün çözünürlükte denetlenmiş; taşma, örtüşme, kesilme, font veya bozuk karakter kusuru görülmemiştir. Bunlar uygulama ya da belge içerik kusuru değil test ortamı/yardımcı komut ret kayıtlarıdır; yeni exact kanal, hedefli/tam regresyon, kaynak bütünlüğü, pre/postflight, yedek, paket ve kurulu UAT kapılarını kaldırmaz.

Bronze 50 installer deneyimi UAT'inin ilk çağrısı canlı paket doğrulayıcısının 4.408 Git blobunu ayrı ayrı okuduğu sessiz aşamada erken kesilmiş ve olay `af2d15fa` boş `wip(rejected)` checkpointiyle korunmuştur. Aynı doğrulama kesilmeden tamamlandığında `governed-preflight.json` 2.795 yönetilen çalışma ağacı dosyasına, paket provenance ise 2.793 izlenen Git dosyasına bağlı olduğu için gerçek stale-preflight FAIL vermiş; sonuç `151384be` ile korunmuştur. İki fazla yol yalnız ignored `scripts/__pycache__/audit-all-project-documents.cpython-312.pyc` ve `scripts/__pycache__/generate-current-master-documentation.cpython-312.pyc` dosyalarıdır. Kalıcı düzeltme `computeGovernedSourceFingerprint` çalışma ağacı taramasında Python `__pycache__` dizinlerini, zaten Git otoritesinde bulunmadıkları için exact dışlar ve regresyon sözleşmesi bu sınırı korur. Eski package/preflight makbuzları devşirilemez; yeni temiz exact committe bütün kayıt, hedefli/tam regresyon, kaynak bütünlüğü, pre/postflight, kaynak koruma, paket ve kurulu UAT yeniden yapılır.

Yeniden üretilen 29 sayfalık ana belge için temas PNG’leri hazırlanırken ilk PowerShell yardımcı çağrısında `System.Drawing.Font` overload seçimi belirsiz kalmış; sayfa renderları sağlam olsa da yardımcı hata `3bc57c12` boş `wip(rejected)` checkpointiyle korunmuştur. Açık `FontFamily`, `Single`, `FontStyle` ve `GraphicsUnit` tipleriyle tekrar edilen temas üretimi üç temas dosyası vermiş; 29/29 sayfa temaslarda, 7–8 ve 27–29. sayfalar özgün çözünürlükte taşma, örtüşme, kesilme, font ve bozuk karakter kusuru olmadan PASS bulunmuştur. Bu test aracı ret kaydı yeni exact teslim zincirini kaldırmaz.

Ana kaynak ve üç kanal `59969f4b` commitine fast-forward edilip manifest 684 ve checkpoint 809 payload yolu her kanalda PASS doğrulandıktan sonra ek kanal verifier'ı zorunlu `--kind` argümanı olmadan çağrılmış ve fail-closed durmuş; test çağrı hatası `2ab9cddf` boş `wip(rejected)` checkpointiyle korunmuştur. Doğru `--kind build` retry; ana kaynak, Bronze, Silver ve Gold'un exact `2ab9cddf` commitinde, doğru dallarda, ortak Git nesne deposunda ve temiz çalışma ağaçlarında olduğunu PASS kanıtlamıştır. İlk ret kanal veya ürün bozulması değildir ve yeni exact teslim zincirini kaldırmaz.

`de6c3c7b` final preflight sertifikası `000a93e7/2792`, aynı commitin exact Git kaynak fingerprinti `f96cd181/2793` üretmiştir. Tek fark daha önce Git tarafından izlenmiş `scripts/__pycache__/generate-master-project-documentation.cpython-313.pyc` dosyasıdır; `.gitignore` izlenen dosyayı untrack etmediği için kapsam düzeltmesi eksik kalmıştır. Gerçek sonuç `1490bb9f` boş `wip(rejected)` checkpointiyle korunur. Bronze 50 paket ve dış append-only geçmişi immutable `REJECTED_INVALID_PACKAGE` olur; üzerine yazılmaz ve trusted runtime sayılmaz. DEC-276/PR-241 yalnız Bronze 51 için rejected-parent history-only lineage, temiz recovery bootstrap ve ardından ayrı same-version maintenance yolu açar; sequence 52+ normal exact continuation kuralı değişmez. Yeni exact hedefli/tam regresyon, kaynak bütünlüğü, preflight/postflight, paket ve kurulu tam UI UAT tamamlanmadan teslim yoktur.

Recovery odaklı test zincirinde `4ec0820d` V29 governed-preflight önkoşulu stale olduğu için test başlamadan durmayı, `de8776f1` ticari temel doğrulayıcısının yeni `IS-0216` sonrasında sabit 60 iş beklemesini, `6a401791` ayrı PowerShell test çağrısında `PPT_WORK_STEP` aktarılmamasını ve `cb7e8749` 65 testten 64'ü PASS iken ledger-drift negatif testinin güvenli genel reddi daha dar hata mesajıyla eşlemesini korur. İş sayısı 61 ve TK-019/IS-0216 bağına getirildi; çağrı ortamı düzeltildi; negatif test beklentisi reddi gevşetmeden iki fail-closed mesajı kabul eder. V29 full preflight 1.248 ticari kontrol, 105 karar ve 2.657 enforcement kontrolü PASS vermiştir. Yeni odaklı retry ve exact kapanış zorunludur.

Recovery kod audit kapanışında `cb7e8749` son rejected checkpoint olarak kalır; ilk 64/65 ile sonraki 5 dosya/65 test ve 9 dosya/82 test ara geçmişi değiştirilmez. Sequence 52+ yolundaki varsayımsal `fileVersion` bağı, mode union `false`/`null` ayrımı, installer öncesi exact `packageVersion`/görünür release bağı, alias-free fixture sözleşmesi, Bronze 51 `IN_PROGRESS` yaşam döngüsü fail-closed kapısı ve immutable parent bundle transaction öncesi canlı path/size/SHA/identity geri-okuması kapatılmıştır. Yeni PR-241 hash ve operation-check zinciriyle en güncel odaklı tur 7 dosya/84 test PASS vermiştir. Exact filtresiz tam regresyon, build, kurulum ve bütün UAT zinciri pendingdir; bu odaklı PASS paket veya teslim yetkisi değildir.

Bu audit kapanışından sonraki hızlı PowerShell yardımcı taraması iki çağrı sınırında fail-closed durmuştur: `ConvertFrom-Json` boş anahtar sınırlaması ve karar kaydı değiştikten sonra zorunlu kural kontrolü yenilenmeden yardımcı taramanın başlatılması. Gerçek çağrı hatası `4c83cee8` boş `wip(rejected)` checkpointiyle korunur ve güncel `latestRejectedCheckpoint` olur; önceki `cb7e8749` tarihsel ret zincirinden çıkarılmaz. Bu olay ürün JSON kusuru değildir, sonraki 7 dosya/84 test odaklı PASS sonucunu geri yazmaz ve kendi başına PASS sayılamaz. Yeni kural kontrolü sonrasında exact filtresiz tam regresyon, build, kurulum ve bütün UAT zinciri yeniden yürütülmeden paket yoktur.

Sonraki master belge üretim çağrısı, PATH'te düz `python` komutu bulunmadığı için içerik üretimine başlamadan fail-closed durmuştur. Gerçek araç çağrısı hatası `aae819cc` boş `wip(rejected)` checkpointiyle kaydedilir ve güncel `latestRejectedCheckpoint` olur; `4c83cee8` ve önceki retler tarihsel zincirde kalır. Aynı üretici doğrulanmış bundled Python ile başarıyla çalışmış, üretilen DOCX 29/29 sayfada temas görselleriyle ve 7–8 ile 27–29. sayfalarda özgün çözünürlükte taşma, örtüşme, kesilme, font ve bozuk karakter olmadan görsel QA PASS vermiştir. Bu belge QA sonucu exact hedefli/tam regresyon, TypeScript, kaynak bütünlüğü, paket ve kurulu uygulama UAT kapılarının yerine geçmez.

Final render zincirindeki ilk retry eski document-skill `scripts/render_docx.py` yolu nedeniyle `ac2c02c1`, ikinci retry eski bundled LibreOffice çalıştırılabilir çözümlemesi nedeniyle `5c07258c` boş `wip(rejected)` checkpointiyle korunmuştur. Güncel `latestRejectedCheckpoint` `5c07258c` olur; bu iki ret de uygulama ya da belge içerik kusuru ve PASS değildir. Güncel document-skill scripti, doğrulanmış `C:\Program Files\LibreOffice\program` ve bundled Poppler ile başarıyla 29 PNG üretmiştir. Beş temas görünümünde bütün sayfalar, ayrıca 1, 7–8 ve 27–29. sayfalar özgün çözünürlükte taşma, örtüşme, kesilme, font ve bozuk karakter olmadan PASS vermiştir.

Görsel PASS sonrasındaki V5 makine doğrulayıcısı, active document set 24.08 işaretine sabit kaldığı için güncel `GUNCEL-2026-08-26-V5` markerını reddetmiştir. Gerçek test hatası `b0615638` boş `wip(rejected)` checkpointiyle korunur ve güncel `latestRejectedCheckpoint` olur. Kök neden belge içeriği değil doğrulayıcı sözleşme driftidir. `config/active-document-set.json` sürüm ve `asOf` alanları 26.08.2026'ya eşlenmiş; Python doğrulayıcı markerı bu kanonik alandan okuyup tarih biçimi ve `asOf` eşliğini kontrol edecek şekilde düzeltilmiştir. Makine retry ve sonraki exact kapanış kapıları PASS olmadan paket yoktur.

Kanonik sürüm düzeltmesi sonrasındaki makine retry 241 kural, 185 karar, 106 ADR, 13 DOCX tablo ve 26 PDF sayfa; V5 governance sözleşmesi 1/1 test PASS vermiştir. Son DOCX renderı 29 sayfa üretmiş; 2–7 önceki onaylı renderla SHA-256 özdeş, değişen 1 ve 8–29 özgün çözünürlükte tek tek kusursuz bulunmuştur. `b0615638` tarihsel rejected checkpoint olarak korunur; master belge kapanışı exact hedefli/tam regresyon, TypeScript, kaynak bütünlüğü, paket ve kurulu uygulama UAT kapılarının yerine geçmez.

`b0615638` sonrasındaki taze odaklı matris, master yönetişim sözleşmesi dahil 8 dosya/85 test PASS vermiştir. Ticari temel doğrulaması ayrıca 1.254 kontrol/87 dosya/61 iş/241 kural PASS olmuştur. Önceki paragraflardaki “son” veya “güncel” nitelemeleri yalnız yazıldıkları tarihsel aşamayı anlatır; aktif son rejected checkpoint `b0615638` olarak kalır. Bu iki PASS exact hedefli/tam regresyon, kaynak bütünlüğü, paket ve kurulu uygulama UAT yetkisi değildir.

Manifest üreticisinin `--help` kipini desteklemeyip dosyaları yazdığı yardımcı çağrı öncesinde ayrı mutation kural kontrolü bulunmadığı için süreç fail-closed reddedilmiş ve `3eec5426` boş `wip(rejected)` checkpointiyle korunmuştur. Aktif son rejected checkpoint budur; ürün kusuru veya PASS değildir. Manifest yalnız kayıt ve indeks kapanışından sonra ayrı mutation kontrolüyle kanonik üretici kullanılarak yeniden üretilecek, ardından exact kapılar aynı temiz committe çalıştırılacaktır.

Master makine doğrulaması PASS sonrasında Vitest 4.1.10'un desteklemediği `--minWorkers` seçeneği çağrıyı test başlamadan durdurmuş ve `86602f7a` boş `wip(rejected)` checkpointiyle korunmuştur. O aşamadaki son rejected checkpoint budur; ürün veya master belge kusuru ve PASS değildir. Governance testi doğru ve desteklenen Vitest çağrısıyla yeniden çalıştırılır.

Doğru Vitest çağrısı ve kayıt kapanışı sonrasında exact Bronze etki değerlendirmesi, `tools/windows-packager/package-lock.json` yolunun fail-closed bağımlılık sicilinde hiçbir kuralla eşlenmediğini saptamıştır. Gerçek kapı reddi `3e496f47` boş `wip(rejected)` checkpointiyle korunur ve aktif son rejected checkpoint olur. Kök neden `tools/` ağacının genel güvenlik ağı dışında kalmasıdır; `tools/` güvenlik ağına, Windows paketleyici manifest/lockfile yolları ayrıca supply-chain, sürüm ve provenance hedefli test bağına alınır. Bu ret ürün çalışma zamanı kusuru veya PASS değildir; güncel exact kanıt zinciri yeni temiz committe baştan çalıştırılır.

Odaklı bağımlılık sicili sözleşme testinde yeni Windows paketleyici yolu doğru iki kuralla eşlenmiş, ancak test `ruleIds` dizisini kanonik üreticinin alfabetik sırasının tersinde beklemiştir. Gerçek 13/14 sonucu `398de9c8` boş `wip(rejected)` checkpointiyle korunur ve aktif son rejected checkpoint olur. Assertion kanonik `governed-source-safety-net`, `isolated-windows-packager-toolchain` sırasına düzeltilir; yeni PASS alınmadan exact kanıt veya paket yetkisi yoktur.

Kanonik sıra düzeltmesi sonrasında odaklı retry 3 dosyada 14/14 test PASS vermiştir. `398de9c8` o aşamadaki son, bugün tarihsel rejected checkpoint olarak kalır; bu odaklı PASS exact hedefli ve filtresiz tam regresyon, kaynak bütünlüğü, pre/postflight, paket veya kurulu uygulama UAT kapılarının yerine geçmez.

Aktif V5 görsel QA çağrısında güncel `render_docx.py` sürümünün desteklemediği yol argümanları `75e4072c`, ardından yanlış tahmin edilen Poppler PATH nedeniyle PDF sayfa sayımının başlayamaması `0f98c7cc` ile ayrı gerçek araç retleri olarak korunur. `0f98c7cc` o aşamadaki son, bugün tarihsel checkpointtir. Workspace bağımlılık kaydından doğrulanan native Poppler ve sistem LibreOffice ile güncel V5 DOCX 29/29 sayfa yeniden üretilmiş ve bütün sayfalar özgün çözünürlükte tek tek taşma, örtüşme, kesilme, font ve bozuk karakter açısından temiz bulunmuştur.

Son kayıt metni eşlemesinde DEC-275/DEC-276 belgeleri değiştirilirken kullanıcı karar sicilindeki belge SHA-256 bağları aynı atomik kümede henüz yenilenmediği için operation-rule-check fail-closed reddetmiştir. Bu FAIL sonrasında generator çağrısının durdurulmaması süreç kusurudur ve `9a370e51` boş `wip(rejected)` checkpointiyle korunur. Aktif son rejected checkpoint budur; ürün, belge veya çalışma zamanı kusuru ve PASS değildir. Karar metni ile sicil hash bağları aynı mutasyonda eşlenir, kapı PASS olmadan hiçbir üretici yeniden başlatılmaz.

`9f16699d` temiz kaynak kapanışından sonra `worktrees:channels` komutu mevcut kanalları fast-forward etmek yerine önce exact HEAD eşliği aradığı için Bronze `89c4332f` üzerinde fail-closed durmuş ve gerçek süreç reddi `dbefb586` boş `wip(rejected)` checkpointiyle korunmuştur. Aktif son rejected checkpoint budur; ürün veya kanal kaynak kusuru ve PASS değildir. Bronze/Silver/Gold önce ayrı açık `ff-only` işlemleriyle nihai exact commite taşınır, ardından setup/eşitlik doğrulayıcısı yeniden çalıştırılır.

Üç kanalın `63c55074` commitine ayrı `ff-only` taşınması, payload hidrasyonu ve exact commit/dal/ortak Git deposu/temiz ağaç doğrulaması PASS olmuştur. Bu committe etki değerlendirmesi ve analizi 105 değişen yol ile 19 hedefli test hesaplamış; hedefli koşu 19 dosya/188 test PASS vermiştir. Filtresiz tam regresyonda 399 dosya ve 2.480 test geçerken `ppk015-network-egress-governance-ratchet.test.ts` tek gerçek FAIL olmuştur. Kök neden `.51` sürüm kimliğini taşıyan `packages/domain/src/app-meta.ts` değişikliğinin PPK-015 üretim kaynak özetini değiştirmesi, ancak güncel ratchet kaydının eski hashte kalmasıdır. Canlı tarama 18 bölge/590 dosya/0 bulgu, 2 external adapter/3 amaç ve 4 yalnız-yerel taşıma dosyasıyla ağ yetkisinin değişmediğini doğrular. FAIL `cc922201` boş `wip(rejected)` checkpointiyle korunur ve aktif son rejected checkpoint olur; ratchet `debfeecf460834f50cf328bff58b2c19ad94ef229610c4c829a35c4331ef235a` canlı özetine eşlenir. Yeni exact hedefli/tam regresyon, TypeScript, kaynak bütünlüğü, pre/postflight, kaynak koruma, paket ve kurulu tam UI UAT PASS olmadan installer yoktur.

PPK-015 kayıt kapanışı sonrasında mevcut V5 DOCX'i belge becerisiyle düzenleme başlangıç makbuzunun ilk çağrısı tek format bekleyen yardımcıya birleşik `docx,pdf` değeri verdiği için içerik üretmeden reddedilmiş ve `df92cdba` boş `wip(rejected)` checkpointiyle korunmuştur. Bu olay ürün, PPK-015 veya belge içerik kusuru ve PASS değildir; aktif son rejected checkpoint olur. Desteklenen `docx` formatı ve tek çıktı sayısıyla beceri başlangıcı başarıyla tamamlanmış, belge üreticisi ancak bundan sonra çalıştırılmıştır. Son render/görsel QA ve bütün exact ürün kapıları tamamlanmadan paket yoktur.

`df92cdba` sonrasinda kapanis metinleriyle yeniden uretilen kanonik V5 DOCX bos cikti dizininde 29 sayfa vermis; sayfa 15 Durum sutunundaki token-ortasi bolunme `787c5570` bos `wip(rejected)` checkpointiyle korunmustur. Uretici gorunen karar durumlarindaki alt cizgileri izinli bosluklara donusturduktan sonra yeniden uretilen final DOCX yine 29 sayfa vermistir. Sayfa 2-7 ve 9-13 onceki onayli renderla byte-exact ayni; degisen 1, 8 ve 14-29 sayfalar ozgun cozulunurlukte tek tek incelenmis, 29/29 tasma, ortusme, kirpilma, font, bozuk karakter ve tablo butunlugu acisindan PASS bulunmustur. Bu belge QA exact urun, paket veya kurulu uygulama UAT kapilarinin yerine gecmez.

`6f9139e1` exact Bronze zincirinde assessment 107 degisen yol/21 hedef test hesaplamis; hedefli 21 dosya/211 test ve filtresiz 399 dosya/2.481 test PASS vermistir. Ardindan source-integrity, manifestin governed preflight tarafindan yeniden uretilen yedi indeks/belge dizini ile ticari temel kanitinin eski SHA-256 degerlerini tasidigini bulmus ve FAIL vermistir. Gercek uretici sirasi kusuru `2abcf853` bos `wip(rejected)` checkpointiyle korunur ve aktif son rejected checkpoint olur. Kok neden manifestin preflighttan once uretilmesi, preflightin ticari kanit ile Git-index tabanli indeksleri sonra yenilemesidir. Final sira belge/ticari kapanis -> governed preflight writer -> manifest/SHA256SUMS son uretim -> exact source-integrity ve read-only pre/postflight olarak sabitlenir; yeni temiz committe tum exact kapilar yeniden PASS olmadan paket yoktur.

`2abcf853` kayıtlarıyla yeniden üretilen V5 DOCX 30 sayfadır. Sayfa 12/14 ilk çoklu görüntü incelemesi önizleme kırpmasını gerçek dosya kırpılması sanmış ve `d1e0b803` boş `wip(rejected)` checkpointiyle korunmuştur. Ayrı `detail=original` inceleme ve piksel ölçümü iki dosyanın da 1547×2002 olduğunu, ilk içeriğin y=53'te ve eksiksiz dört sütunlu kahverengi başlığın y=183–209/x=183–1363 aralığında bulunduğunu doğrulamış; ilk bulgu geri çekilmiş ve nihai V5 sonucu 30/30 PASS olmuştur. Checkpoint tarihsel yanlış pozitif inceleme kaydıdır; ürün veya belge kusuru değildir.

Görsel turdaki içerik sürekliliği notu üzerine yapılan kaynak incelemesi, `docs/10_MASTER_DECISION_REGISTER.md` içinde `ADR-067 bağlayıcıdır` denmesine karşın `docs/adr/ADR-067-*.md` kaydının hiç bulunmadığını göstermiştir. Gerçek kaynak kayıt bütünlüğü FAIL'i `f1590772` boş `wip(rejected)` checkpointiyle korunur ve aktif son rejected checkpoint olur. ADR-067, DEC-084 ile mevcut Migrasyon 38/repository claim rezervasyonu davranışından geri kurulur. Operation-rule-check ana karar sicilindeki ADR referanslarını gerçek kaynak dosyalarına ve ADR-001'den en yüksek ADR'ye kesintisiz numara dizisine bağlar; V5 verifier aynı kesintisizliği doğrular. Exact test, indeks, master, manifest ve UAT zinciri yeni temiz committe baştan çalıştırılmadan paket yoktur.

ADR-067 kapanışıyla üretilen güncel master PDF Poppler ile 26 PNG'ye render edilmiştir. Sayfa 1–3 ve 6–26 görsel bütünlükte PASS; sayfa 4/5'te `Yerel` sütunundaki uzun makine durum kodları dar alanda identifier ortasından bölünmüş (`FAIL_CLO` / `SED`, `ACCEPTA` / `NCE_INCOMPLETE`) ve okunabilirlik kapısını geçmemiştir. Gerçek görsel FAIL `6d94ab9e` boş `wip(rejected)` checkpointiyle korunur ve aktif son rejected checkpoint olur. Kanonik JSON değerleri değişmez; yalnız DOCX/PDF sunum katmanı alt çizgileri anlamsal boşluklara dönüştürür. Yeni DOCX/PDF üretimi, 30/30 ve 26/26 görsel QA, exact test/bütünlük ve kurulu UAT tamamlanmadan paket yoktur.

Görünür durum düzeltmesi sonrasında yeni DOCX 30/30 ve PDF 26/26, toplam 56/56 sayfa özgün çözünürlükte ayrı ayrı incelenmiş ve PASS vermiştir. Önceki PDF 4/5 `WIT/H`, `CLO/SED`, `ACCEPTA/NCE` bölünmeleri yoktur; durumlar yalnız anlamsal boşluklardan sarılır. ADR-067 her iki formatta ADR-066/068 arasında exact başlık/yol ile görünür. Taşma, örtüşme, kırpılma, font/glyph, bozuk karakter, tablo başlığı/sınırı, footer ve sayfa numarası kusuru bulunmamıştır. Bu belge QA exact ürün, paket ve kurulu uygulama UAT kapılarının yerine geçmez.

Final-freeze turunun ilk `render_docx.py` çağrısı aracın desteklemediği `--libreoffice_path` ve `--poppler_path` seçenekleriyle başlatıldığı için render başlamadan durmuş; çağrı gerçeği `764e856b` boş `wip(rejected)` checkpointiyle korunmuştur. Doğru retry LibreOffice ve Poppler yollarını `PATH` üzerinden sağlamış, DOCX 30 ve PDF 26 sayfayı eksiksiz üretmiştir. Bu kayıt ürün veya belge FAIL'i değildir; çağrı hatasının gizlenmediğini ve doğru tekrarın ayrı kanıtlandığını gösterir.

Final-freeze2 çoklu görüntü denetiminde DOCX sayfa 2/7 header-footer/marjı ve PDF çift sayfa footerları kırpılmış sanılmıştır. Aynı PNG'lerin tam sayfa özgün çözünürlük readback'i bütün header/footer/marjların eksiksiz olduğunu doğrulamış; bu bölüm önizleme kırpması yanlış pozitifidir. PDF sayfa 9 DEC dizinindeki uzun exact yolların `sta`/`tus`, `uni`/`queness` gibi karakter ortasından sarılması ise gerçek okunabilirlik FAIL'idir. Bütün sonuç `17ad92d0` boş `wip(rejected)` checkpointiyle korunur. PDF karar ve ADR yol hücreleri exact metni değiştirmeyen sıfır-genişlikli ayraç-sonrası satır kırma noktalarıyla oluşturulur; taze tam render/görsel QA PASS olmadan belge kapanışı veya paket yoktur.

Final-freeze3 makine doğrulaması 30 DOCX ve 27 PDF sayfayı eksiksiz bulmuş, fakat U+200B ayraç noktaları ReportLab'in uzun sözcük bölmesini önlememiştir. PDF karar/ADR dizinlerinde `root-caus`/`e-remediation.md`, `boundar`/`ies.md` ve `.m`/`d` örnekleri ayraç dışı token-ortası okunabilirlik FAIL'idir. Sonuç `a0d9df42` boş `wip(rejected)` checkpointiyle korunur. Kalıcı düzeltme yolu regex ile yalnız `/`, `-`, `_` sonrasından parçalara ayırır, parçaları en çok 48 karakterlik satırlara toplar ve gerçek satır sonunu yalnız bu güvenli ayraç sınırlarına koyar. Yeni tam render/görsel QA zorunludur.

Final-freeze4 deterministik ayraç satırlarıyla yeniden üretilmiş; DOCX 30/30 ve PDF 27/27, toplam 57/57 sayfa tam tek-sayfa özgün çözünürlükte bağımsız olarak incelenmiş ve PASS vermiştir. PDF karar/ADR yolları yalnız `/`, `-`, `_` sonrasında sarılır; `.m`/`d` veya kelime ortası bölünme yoktur. ADR-067 iki çıktıda 066/068 arasında exact başlık/yol ile görünür. Taşma, örtüşme, kırpılma, font/glyph, bozuk karakter, tablo başlığı/sınırı, header/footer, marj ve sayfa numarası kusuru bulunmamıştır. Bu QA exact ürün, paket ve kurulu uygulama UAT kapılarının yerine geçmez.

`0099e39e` yanlış ana kaynak kökünde başlatılan mutation assessment çağrısını fail-closed ret olarak korur ve aktif son rejected checkpoint olur; aynı commitin exact Bronze çalışma ağacındaki doğru retryı 109 değişen yol/21 hedef test, güncel analysis ise 109 yol PASS vermiştir. Paralel kaynak denetimi, assessment `sourceCommit` ve `baselineCommit` alanlarının gerçek diff/test/paket otoritesini değiştiremese de validator tarafından doğrudan exact eşlenmediğini saptamıştır. P2 kayıt bütünlüğü boşluğu kapatılmıştır: source commit canlı release provenance HEAD, baseline commit doğrulanmış harici baseline pointer HEAD ile 40–64 hex ve byte-exact eşleşmeden create-assessment, create-analysis, postflight, builder, canlı/tarihsel package provenance ve final readiness ilerlemez. Dört stale/eksik negatif vaka ile yedi çağrı noktasının statik sözleşmesi odaklı 2 dosya/8 test PASS vermiştir. Bu PASS yeni exact committe assessment/analysis, hedefli/tam regresyon, kaynak bütünlüğü, pre/postflight, kaynak koruma, paket ve kurulu tam UI UAT zorunluluğunu kaldırmaz.

27.08.2026 tarihinde `.51` UAT110 çağrısı iki uzun provenance doğrulamasını geçip exclusive evidence guard oluşturduktan sonra kurulum başlamadan Windows uninstall state-snapshot aşamasında durmuştur. Windows PowerShell Operational Event 4100 exact `PropertyNotFoundStrict` ve eksik `DisplayName` alanını göstermiştir. `059e3787` canlı UAT ret kaydıdır; `a1aec744`, `8472face`, `15e3c9d0` ve `64695e58` düzeltme sırasında yakalanan strict-mode ve davranış testi retlerini korur. Hiçbir installer fazı başlamamış, kurulu EXE/UAT111 oluşmamış ve 15 mevcut kullanıcı dosyası değişmemiştir. Registry okuması yalnız eksik isteğe bağlı alanı boş değere normalleştirir, gerçek accessor hatasını fail-closed bırakır; sıfır kayıt sonucu kesin dizi olur. Windows PowerShell 5.1 strict-mode canlı snapshot ve sözleşme matrisi 2 dosya/17 test PASS vermiştir. `15e3c9d0` PRE_MUTATION baseline PASS'tir; Bronze `27.08.2026.52` için exact assessment, hedefli/tam test, kaynak bütünlüğü ve kurulu N→N+1 UAT zinciri yeniden üretilmeden kapanış yoktur.

Final-freeze6 P2 kayit sertlestirmesi sonrasinda kanonik V5 DOCX 30 ve PDF 27 sayfa uretmistir. Onceki final-freeze5 onayina gore 25 sayfa byte-exact aynidir; degisen DOCX 1 ve 9-19 ile PDF 1 ve 9-27 sayfalari uc bagimsiz gorsel denetimde 32/32 PASS bulunmustur. Toplam 57/57 sayfada tasma, ortusme, kirpilma, font/glyph, bozuk karakter, tablo, header/footer, marj, sayfa numarasi veya guvenli ayirac disi token bolunmesi yoktur; ADR-067 sira ve exact yol bagi korunur. Bu kayit exact hedefli/tam regresyon, kaynak butunlugu, pre/postflight, kaynak koruma, paket ve kurulu tam UI UAT kapilarinin yerine gecmez.

Bronze 52 exact hedefli kanıt üreticisinin ilk çağrısı zorunlu açık test dosyası listesi verilmeden test başlamadan durmuş ve `d68fd2a4` boş checkpointiyle korunmuştur. Bu kayıt invocation-only'dir; PASS veya gerçek test sonucu değildir. Doğru çağrı `d68fd2a4` exact kaynağında 19 hedef dosyayı çalıştırmış, 189 test PASS verirken `operation-rule-check-policy.test.ts` içindeki PR-240 yaşam döngüsü kapısı `packageStatus` alanının zorunlu fail-closed `BLOCKED_`/`FAIL_` ön ekini taşımamasını reddetmiştir. Tek gerçek test FAIL'i `3976994d` boş `wip(rejected)` checkpointiyle korunur ve aktif son rejected checkpoint olur. Test gevşetilmez; Bronze 51 immutable kanıtı korunarak Bronze 52 NOT_BUILT durumu `BLOCKED_` ön ekiyle ifade edilir. Yeni exact etki, hedefli/tam regresyon, TypeScript, sözdizimi, kaynak bütünlüğü, pre/postflight, kaynak koruma, paket ve kurulu tam UI UAT zinciri yeniden tamamlanmadan teslim yoktur.

Paket öncesi canlı predecessor denetiminde Bronze 51 uygulama EXE'si immutable bundle ile exact eşleşirken Windows Apps & Features uninstall girdisinin sürüm ekli `DisplayName` değeri eski `Bronze$` eşlemesinde legacy sayılmış, `InstallLocation` alanı boş ve `DisplayIcon` alanı `uninstallerIcon.ico` bulunmuştur. Bu durum yeni `.52` UAT110 sicil kimlik kapısını düşürecekti. Kalıcı düzeltme, electron-builder'ın exact `ParsYuva Aile Yaşam Merkezi <Kanal> <paket-sürümü>` biçimini sınıflandırır ve public `UNINSTALL_REGISTRY_KEY` girdisini `customInstall` sonunda kanonik `$INSTDIR` ile gerçek `${PPT_INSTALLER_EXECUTABLE},0` değerleriyle tamamlar. DisplayName, DisplayVersion, DisplayIcon, normal/sessiz uninstall komutları exact karşılaştırılır; builder template hook sırası fail-closed doğrulanır. Sözleşme testi ve gerçek N→N+1/maintenance readback PASS olmadan paket kabul edilmez.

İlk exact sürüm ekli `DisplayName` sınıflandırması kaynakta doğrudan `Yaşam` Unicode literali taşımış, Windows PowerShell 5.1 canlı davranış testinde Bronze uninstall girdisi yine sınıflandırılamamıştır. Odaklı tur 2 dosyada 17 test PASS ve 1 gerçek test FAIL vermiş; sonuç `fb8683dc` boş `wip(rejected)` checkpointiyle korunmuş ve aktif son rejected checkpoint olmuştur. Ürün adı kaynak dosya kodlamasına bağlı kalmadan ASCII metin ile `[char]0x015F` birleşiminden exact oluşturulur; test gevşetilmez ve taze exact zincir yeniden çalıştırılır.

Kaynak-ASCII `[char]0x015F` düzeltmesi sonrası packageStatus ve Windows kurulu sürüm UAT sözleşmelerini kapsayan 2 dosya/18 test PASS vermiştir. Bu odaklı PASS gerçek kurulu `.52` sicil readback'i veya yeni temiz exact tam kapanış yerine geçmez.

İlk TICARI-052 retryı uninstall sicili bağında doğru semantiği taşıyan iki ifadenin doğrulayıcıyla sözcük sırası farklı olduğu için fail-closed durmuş ve `a6020cb4` boş `wip(rejected)` checkpointiyle korunmuştur. Aktif son rejected checkpoint budur. Kayıt exact `bos InstallLocation` ve `gercek uygulama EXE kimligini yazar` sözleşmesine eşlenir; taze ticari test olmadan PASS sayılmaz.

İkinci TICARI-052 retryı, güncel metin eklenirken eski zorunlu `2 dosya/17 test PASS`, `15e3c9d0 pre-mutation baseline PASS` ve `d68fd2a4 test baslamadan duran invocation-only checkpointtir` bağlarının kayıttan düşürüldüğünü bulmuş ve `61f09ed5` ile korunmuştur. Aktif son rejected checkpoint budur. Eski/güncel exact markerların tamamı aynı kayıt içinde geri kurulur ve test yeniden çalıştırılır.

Bütün TICARI-052 markerları geri kurulduktan sonraki retry ticari temel 1.254 kontrol/87 dosya/61 iş/241 kural PASS vermiştir. Bu odaklı kayıt PASS'i yeni temiz exact committe hedefli/tam regresyon, bütünlük, paket veya kurulu UAT yerine geçmez.

Ana kaynak ve Bronze/Silver/Gold `f4f84896` commitinde temiz exact eşitlenmiş; salt-okunur governed preflight ile 88 değişen yol/19 hedef test etki değerlendirmesi PASS vermiştir. Hedefli tur 19 dosya/191 test PASS'tir. Filtresiz turda 399 dosya/2.483 test PASS olurken yalnız PPK-015 current source inventory SHA ratchet'i eski kalmış ve gerçek FAIL `8ea2dfe1` boş `wip(rejected)` checkpointiyle korunmuştur. Canlı sınır 18 bölge/590 dosya/0 bulgu/2 adapter/3 amaç/4 yalnız-yerel taşıma dosyasıdır; yeni kaynak özeti `f54e3f302649af67ed6d028e66673eea68b0d58c2ba43c912c1ccb7534babe98`, yetkili adapter/purpose özeti değişmemiştir. Ağ yetkisi genişlememiştir. Ratchet, bütün bağımlı kayıtlar ve üretilmiş kanıtlar aynı mutasyonda eşlenir; taze exact hedefli/tam regresyon, TypeScript, kaynak bütünlüğü, pre/postflight, paket ve kurulu UAT zinciri yeniden tamamlanmadan teslim yoktur.

İlk ratchet contract retryı 43 kontrolde 42 PASS/1 FAIL vermiştir: son migration 121 bilgisi doğruyken zorunlu exact küçük harfli `migration 117` tarihsel ayırıcısı metinden düşmüştür. Gerçek test hatası `24e6bd71` boş `wip(rejected)` checkpointiyle korunur ve aktif son rejected checkpoint budur. Ayırıcı geri kurulur; PPK-015 canlı sınırı, yetkili adapter/purpose özeti ve ağ yetkisi değişmez. Contract/runtime, ticari doğrulama ve bütün exact zincir yeniden çalıştırılmadan PASS yoktur.

Ayırıcı düzeltmesi sonrası retry contract 43/43, runtime 10/10, iki odaklı test dosyası 23/23 ve ticari temel 1.254 kontrol PASS vermiştir. `24e6bd71` tarihsel gerçek ret olarak kalır; bu odaklı PASS yeni exact committe kanonik hedefli/tam regresyon, bütünlük, paket ve kurulu UAT yerine geçmez.

Bu kayıtlarla yeniden üretilen güncel master DOCX 31/31 ve PDF 28/28, toplam 59/59 sayfada görsel QA PASS vermiştir. Bütün sayfalar temas görünümünde, değişiklik yoğun 7–9 ve tablo yoğun 25–31 ayrıca özgün çözünürlükte incelenmiş; taşma, örtüşme, kırpılma, token-ortası sarım, font/glyph, tablo, header/footer, marj veya sayfa numarası kusuru bulunmamıştır. Belge PASS'i exact ürün ve kurulu UAT yerine geçmez.

`e0f85425` exact Bronze 52 turunda hedefli 21 dosya/214 test, filtresiz 399 dosya/2.484 test ve root TypeScript PASS vermiştir. Bunun ardından TypeScript ön-kapıları 10 izlenen kanıt dosyasında yalnız `generatedAt` alanını değiştirmiş; temiz çalışma ağacı ve source-integrity kapısı `Release source worktree is not clean.` sonucu ile FAIL olmuş ve gerçek ret `77a87a87` ile korunmuştur. Bu driftler ürün davranışı veya PASS değildir. On üretici strict `--no-write` sözleşmesine, pretypecheck/prebuild exact aktarımına ve fail-closed kural/test bağına alınır; 10 dosya byte-exact değişmeden yeni exact zincir tamamlanmadan paket yoktur.

İlk yeni exact Bronze preflight, release klasöründe kalan `.51` EXE ve blockmap artefaktlarını PR-229 ihlali olarak reddetmiş ve gerçek FAIL `4462706a` ile korunmuştur. Eski installer EXE, blockmap ve builder-debug çıktısı exact Bronze release kökünden silinir; kurulu `.51` predecessor ile immutable release-history provenance bundle korunur. Ardından yanlış `--no-write` bayrağının governed preflight tarafından reddedilmeyip writer moda düşmesi on izlenen indeks/kanıt dosyasını değiştirmiş ve `0854a4ec` ile korunmuştur. Preflight artık yalnız argümansız writer veya exact `--read-only` çağrısını kabul eder; başka ya da yinelenen bütün bayraklar yazım başlamadan fail-closed reddedilir. Negatif CLI kapısı ve odaklı 2 dosya/12 test PASS'tir; değişen Bronze kanıt bytesları exact HEAD'e geri alınır ve bütün exact zincir yeni temiz committe yeniden başlar.
