# ADR-104 — Monthly Channel Release Identity

Use `Bronze 04.08.2026.27` as the visible identity and `4.8.2026-27` as the internal npm-compatible package version. Monthly sequence resets each calendar month but continues from the number of actual releases already produced in that month. Historical build records remain immutable history and do not define the active visible naming scheme.
