import assert from 'node:assert/strict';
import { execFileSync } from 'node:child_process';
import { DatabaseSync } from 'node:sqlite';
import { mkdir, readFile, rm, writeFile } from 'node:fs/promises';
import { dirname, join, resolve } from 'node:path';
import { pathToFileURL } from 'node:url';

const tmp=join(process.cwd(),'.tmp','build190-clean-rewrite-terminal-chronology-sqlite-runtime');
const out=resolve(process.argv[2]??'artifacts/validation/build190-clean-rewrite-terminal-chronology-sqlite-runtime.json');
await rm(tmp,{recursive:true,force:true});await mkdir(tmp,{recursive:true});
const globalRoot=execFileSync('npm',['root','-g'],{encoding:'utf8'}).trim();const ts=(await import(pathToFileURL(join(globalRoot,'typescript','lib','typescript.js')).href)).default;
const transpile=async(path,name,rewrite=(text)=>text)=>{const source=await readFile(path,'utf8');const code=rewrite(ts.transpileModule(source,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext}}).outputText);const target=join(tmp,name);await writeFile(target,code);return target;};
const repositoryPath=await transpile('packages/repositories/src/backup-propagation-repository.ts','repository.mjs',code=>code.replace(/import \{ SqliteRepository \} from '\.\/sqlite-base\.js';/,`class SqliteRepository { database(context){return context.transaction;} execute(context,operation){try{context.transaction.exec('BEGIN');const value=operation();context.transaction.exec('COMMIT');return {ok:true,value};}catch(error){try{context.transaction.exec('ROLLBACK');}catch{}return {ok:false,error};}} }`));
const servicePath=await transpile('apps/desktop/src/main/automatic-clean-backup-rewrite-service.ts','service.mjs');
const {SqliteBackupPropagationRepository}=await import(pathToFileURL(repositoryPath).href);const {AutomaticCleanBackupRewriteService}=await import(pathToFileURL(servicePath).href);
const migrationSource=await readFile('packages/database/src/family-database-migrations.ts','utf8');const extract=(name)=>{const start=`const ${name} = `+'`';const from=migrationSource.indexOf(start);if(from<0)throw new Error(`${name} missing`);const body=from+start.length;const end=migrationSource.indexOf('`;\n',body);return migrationSource.slice(body,end);};
const schema=(db)=>{db.exec(`PRAGMA foreign_keys=ON;CREATE TABLE database_metadata(key TEXT PRIMARY KEY,value TEXT NOT NULL,updated_at TEXT NOT NULL);INSERT INTO database_metadata VALUES('schema_generation','test','2026-07-31T00:00:00.000Z');CREATE TABLE backup_propagation_runs(id TEXT PRIMARY KEY,status TEXT,pending_records INTEGER,target_count INTEGER,refreshed_targets INTEGER,quarantined_artifacts INTEGER,pending_remaining INTEGER,manual_backup_warning INTEGER,target_results TEXT,error TEXT,started_at TEXT,completed_at TEXT);CREATE TABLE backup_clean_rewrite_policy(id TEXT PRIMARY KEY,enabled INTEGER NOT NULL,retention_days INTEGER NOT NULL,manual_failure_backoff_minutes INTEGER NOT NULL,automatic_failure_backoff_minutes INTEGER NOT NULL,high_load_defer_minutes INTEGER NOT NULL,state TEXT NOT NULL,consecutive_failures INTEGER NOT NULL,last_outcome TEXT NOT NULL,last_trigger TEXT,last_attempt_at TEXT,last_success_at TEXT,next_attempt_at TEXT,last_error TEXT,in_progress_run_id TEXT,in_progress_started_at TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL);CREATE TABLE backup_clean_rewrite_runs(id TEXT PRIMARY KEY,trigger TEXT NOT NULL,status TEXT NOT NULL,retention_cutoff TEXT NOT NULL,due_records INTEGER NOT NULL,enabled_targets INTEGER NOT NULL,propagation_run_id TEXT,next_attempt_at TEXT,error TEXT,started_at TEXT NOT NULL,completed_at TEXT,updated_at TEXT NOT NULL);CREATE TABLE data_lifecycle(resource_type TEXT,resource_id TEXT,state TEXT,backup_propagation_pending INTEGER,purged_at TEXT,updated_at TEXT);`);for(const name of ['cleanBackupRewriteLinkedChronologySql','cleanBackupRewriteRecoveryChronologySql','cleanBackupRewriteClaimChronologySql','cleanBackupRewriteOperationalIsolationSql'])db.exec(extract(name));};
const insertPolicy=(db)=>db.prepare(`INSERT INTO backup_clean_rewrite_policy VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).run('default',1,30,60,360,30,'idle',0,'never',null,null,null,null,null,null,null,'2026-07-01T00:00:00.000Z','2026-07-31T03:00:00.000Z');
const context=(db)=>({transaction:db,actor:{userId:'admin',roles:['family_admin']},correlationId:'build190',occurredAt:'2026-07-31T03:00:00.000Z'});
class Store{constructor(db,{deferred=false,targets=1,throwError=false}={}){this.db=db;this.repo=new SqliteBackupPropagationRepository();this.ctx=context(db);this.deferred=deferred;this.targets=targets;this.throwError=throwError;this.diagnostics=[];}unwrap(result){if(!result.ok)throw result.error;return result.value;}getBackupCleanRewritePolicy(){return this.unwrap(this.repo.getCleanRewritePolicy(this.ctx));}getBackupCleanRewriteStatus(at){return{policy:this.getBackupCleanRewritePolicy(),pendingRecords:1,dueRecords:1,enabledTargets:this.targets,adaptiveDeferred:this.deferred,...(this.deferred?{adaptiveReason:'CPU %91'}:{}),checkedAt:at};}listBackupCleanRewriteRuns(limit=20){return this.unwrap(this.repo.listCleanRewriteRuns(this.ctx,limit));}updateBackupCleanRewritePolicy(){throw new Error('not used');}claimBackupCleanRewrite(input){return this.unwrap(this.repo.claimCleanRewrite(this.ctx,input));}completeBackupCleanRewrite(input){return this.unwrap(this.repo.completeCleanRewrite(this.ctx,input));}recoverInterruptedBackupCleanRewrite(observedAt,error){return this.unwrap(this.repo.recoverInterruptedCleanRewrite(this.ctx,{observedAt,error})).policy;}propagatePurgedDataToManagedBackups(){if(this.throwError)throw new Error('disk failure');throw new Error('unexpected propagation');}recordDiagnostic(severity,code,message,details){this.diagnostics.push({severity,code,message,details});}}
const mono=(values)=>{let i=0;return()=>values[Math.min(i++,values.length-1)];};const checks=[];const check=(label,fn)=>{fn();checks.push(label);};const start='2026-07-31T03:00:00.000Z';

let db=new DatabaseSync(':memory:');schema(db);insertPolicy(db);let store=new Store(db,{deferred:true});let service=new AutomaticCleanBackupRewriteService(()=>store,()=>start,mono([100,4100]));let result=service.runAutomaticCycle();let policy=db.prepare(`SELECT * FROM backup_clean_rewrite_policy`).get();let run=db.prepare(`SELECT * FROM backup_clean_rewrite_runs`).get();
check('SQLite deferred status persisted',()=>assert.equal(run.status,'deferred'));check('SQLite deferred completion is monotonic',()=>assert.equal(run.completed_at,'2026-07-31T03:00:04.000Z'));check('SQLite deferred policy timestamp matches run',()=>assert.equal(policy.updated_at,run.completed_at));check('SQLite deferred retry anchors to completion',()=>assert.equal(policy.next_attempt_at,'2026-07-31T03:30:04.000Z'));check('service deferred result matches SQLite',()=>assert.equal(result.rewriteRun.completedAt,run.completed_at));db.close();

db=new DatabaseSync(':memory:');schema(db);insertPolicy(db);store=new Store(db,{targets:0});service=new AutomaticCleanBackupRewriteService(()=>store,()=>start,mono([500,5500]));result=service.runAutomaticCycle();policy=db.prepare(`SELECT * FROM backup_clean_rewrite_policy`).get();run=db.prepare(`SELECT * FROM backup_clean_rewrite_runs`).get();check('SQLite attention status persisted',()=>assert.equal(run.status,'attention'));check('SQLite attention completion is monotonic',()=>assert.equal(run.completed_at,'2026-07-31T03:00:05.000Z'));check('SQLite attention retry is six hours later',()=>assert.equal(run.next_attempt_at,'2026-07-31T09:00:05.000Z'));check('SQLite attention policy and run agree',()=>assert.deepEqual([policy.state,policy.last_outcome],['attention','attention']));db.close();

db=new DatabaseSync(':memory:');schema(db);insertPolicy(db);store=new Store(db,{throwError:true});service=new AutomaticCleanBackupRewriteService(()=>store,()=>start,mono([900,8900]));result=service.runManual();policy=db.prepare(`SELECT * FROM backup_clean_rewrite_policy`).get();run=db.prepare(`SELECT * FROM backup_clean_rewrite_runs`).get();check('SQLite failed status persisted',()=>assert.equal(run.status,'failed'));check('SQLite failed completion is monotonic',()=>assert.equal(run.completed_at,'2026-07-31T03:00:08.000Z'));check('SQLite manual retry is one hour later',()=>assert.equal(policy.next_attempt_at,'2026-07-31T04:00:08.000Z'));check('SQLite failure error retained',()=>assert.equal(run.error,'disk failure'));check('SQLite terminal timestamps agree',()=>assert.equal(policy.updated_at,run.updated_at));db.close();

db=new DatabaseSync(':memory:');schema(db);insertPolicy(db);store=new Store(db,{deferred:true});service=new AutomaticCleanBackupRewriteService(()=>store,()=>start,mono([1000,999,999]));assert.throws(()=>service.runAutomaticCycle(),/geriye gidemez/);checks.push('SQLite backward monotonic denied');policy=db.prepare(`SELECT * FROM backup_clean_rewrite_policy`).get();run=db.prepare(`SELECT * FROM backup_clean_rewrite_runs`).get();check('SQLite backward denial keeps owner running',()=>assert.equal(policy.state,'running'));check('SQLite backward denial keeps ledger running',()=>assert.equal(run.status,'running'));const recovered=service.recoverInterrupted();policy=db.prepare(`SELECT * FROM backup_clean_rewrite_policy`).get();run=db.prepare(`SELECT * FROM backup_clean_rewrite_runs`).get();check('SQLite recovery clears owner',()=>assert.equal(recovered.state,'backoff'));check('SQLite recovery marks interrupted',()=>assert.equal(run.status,'interrupted'));check('SQLite recovery retry uses persisted floor',()=>assert.equal(policy.next_attempt_at,'2026-07-31T09:00:00.000Z'));db.close();

assert.equal(checks.length,20);const report={schemaVersion:1,product:'Anadolu Parsı Aile Yaşam Merkezi',featureBuild:190,stage:'Bronze RC2 Active Development',scope:'Repository-backed SQLite persistence of monotonic clean-rewrite terminal chronology',status:'PASS',checks:checks.length,checkLabels:checks,nodeSqlite:'node:sqlite DatabaseSync',generatedAt:new Date().toISOString()};await mkdir(dirname(out),{recursive:true});await writeFile(out,`${JSON.stringify(report,null,2)}\n`);await rm(tmp,{recursive:true,force:true});console.log(`Build 190 clean rewrite terminal chronology SQLite runtime: PASS (${checks.length}/${checks.length})`);
