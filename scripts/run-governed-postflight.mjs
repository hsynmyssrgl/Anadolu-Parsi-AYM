import { spawnSync } from 'node:child_process';import { mkdir, writeFile } from 'node:fs/promises';import { readJson } from './lib/governance-utils.mjs';
const registry=await readJson('config/canonical-rule-registry.json');const acknowledgement=await readJson('config/rule-acknowledgement.json');const supplied=process.env.PPT_RULES_SHA256;if(acknowledgement.release!==registry.effectiveRelease||acknowledgement.rulesSha256!==registry.rulesSha256){console.error('GOVERNED_POSTFLIGHT blocked: rule acknowledgement is missing or stale');process.exit(1)}if(supplied!==undefined&&supplied!==registry.rulesSha256){console.error('GOVERNED_POSTFLIGHT blocked: supplied rule SHA does not match the canonical registry');process.exit(1)}
const commands=[['scripts/run-governed-preflight.mjs'],['scripts/verify-universal-rule-enforcement.mjs'],['scripts/verify-step-checkpoint-gate.mjs'],['scripts/verify-delivery-report-contract-v2.mjs'],['scripts/generate-project-artifact-index-v2.mjs'],['scripts/verify-project-artifact-index-v2.mjs']];const results=[];let failed=false;for(const args of commands){const p=spawnSync(process.execPath,args,{encoding:'utf8',env:process.env});results.push({script:args[0],exitCode:p.status,stdout:p.stdout.trim(),stderr:p.stderr.trim()});if(p.status!==0){failed=true;break}}
const report={schemaVersion:1,release:registry.effectiveRelease,rulesSha256:registry.rulesSha256,status:failed?'FAIL':'PASS',results,generatedAt:new Date().toISOString()};await mkdir('artifacts/validation',{recursive:true});await writeFile('artifacts/validation/governed-postflight.json',JSON.stringify(report,null,2)+'\n');for(const r of results)console.log(`${r.script}: ${r.exitCode===0?'PASS':'FAIL'}`);if(failed){console.error(results.at(-1)?.stderr||results.at(-1)?.stdout||'postflight failed');process.exit(1)}console.log(`GOVERNED_POSTFLIGHT: PASS / rules ${registry.rulesSha256}.`);
