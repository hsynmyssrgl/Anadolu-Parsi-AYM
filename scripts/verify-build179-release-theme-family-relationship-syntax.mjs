import { createRequire } from 'node:module';
import { execFileSync,spawnSync } from 'node:child_process';
import { mkdir,readFile,writeFile } from 'node:fs/promises';
import { dirname,join } from 'node:path';
const out=process.argv[2]??'artifacts/validation/build179-release-theme-family-relationship-syntax.json';const checks=[];const check=(name,condition,error)=>checks.push({name,status:condition?'PASS':'FAIL',...(condition||!error?{}:{error})});const run=(name,args)=>{const result=spawnSync(process.execPath,args,{encoding:'utf8'});check(name,result.status===0,`${result.stdout}\n${result.stderr}`.trim());};
run('package source controlled TypeScript',['scripts/verify-package-source-types.mjs']);run('desktop main controlled TypeScript',['scripts/verify-desktop-main-source-types.mjs']);
const require=createRequire(import.meta.url);const globalRoot=execFileSync('npm',['root','-g'],{encoding:'utf8'}).trim();const ts=require(join(globalRoot,'typescript'));
for(const [name,path,kind] of [['relationship catalog TypeScript syntax','packages/domain/src/family-relationship-catalog.ts',ts.ScriptKind.TS],['application use case TypeScript syntax','packages/application/src/family-use-cases.ts',ts.ScriptKind.TS],['renderer TSX syntax','apps/desktop/src/renderer/App.tsx',ts.ScriptKind.TSX]]){const source=await readFile(path,'utf8');const sourceFile=ts.createSourceFile(path,source,ts.ScriptTarget.ES2024,true,kind);const diagnostics=sourceFile.parseDiagnostics??[];check(name,diagnostics.length===0,diagnostics.map(item=>ts.flattenDiagnosticMessageText(item.messageText,'\n')).join('\n'));}
const failures=checks.filter(item=>item.status==='FAIL');const report={schemaVersion:1,product:'Anadolu Parsı Aile Yaşam Merkezi',build:179,status:failures.length?'FAIL':'PASS',checks:checks.length,passed:checks.length-failures.length,failures,scenarios:checks,generatedAt:new Date().toISOString()};await mkdir(dirname(out),{recursive:true});await writeFile(out,`${JSON.stringify(report,null,2)}\n`);if(failures.length){console.error(JSON.stringify(report,null,2));process.exit(1);}console.log(`Build 179 release theme and family relationship syntax: PASS (${checks.length}/${checks.length})`);
