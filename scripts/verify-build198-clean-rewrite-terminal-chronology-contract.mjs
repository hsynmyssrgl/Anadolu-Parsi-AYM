import assert from 'node:assert/strict';import {readFile,mkdir,writeFile} from 'node:fs/promises';import {dirname,resolve} from 'node:path';
const out=resolve(process.argv[2]??'artifacts/validation/build198-clean-rewrite-terminal-chronology-contract.json');const files=['packages/database/src/family-database-migrations.ts','packages/repositories/src/backup-propagation-repository.ts','docs/CLEAN_BACKUP_REWRITE_TERMINAL_CHRONOLOGY_MONOTONICITY_V1.md','docs/adr/ADR-071-terminal-clean-rewrite-chronology-monotonicity.md','docs/10_MASTER_DECISION_REGISTER.md','BUILD_STATUS_BRONZE_RC2_BUILD198.md'];const text=(await Promise.all(files.map((file)=>readFile(file)))).map(x=>x.toString()).join('\n');const patterns=[/REVISION-198-CLEAN-BACKUP-TERMINAL-CHRONOLOGY-MONOTONICITY/,/createMigrationDefinition\(42/,/terminal completion precedes start/,/recovered completion precedes start/,/tamamlanma zamanı çalışma başlangıcından önce olamaz/,/DEC-088/,/ADR-071/,/31\.07\.2026\.198/];for(const p of patterns)assert.match(text,p);const report={schemaVersion:1,featureBuild:198,status:'PASS',checks:patterns.length,generatedAt:new Date().toISOString()};await mkdir(dirname(out),{recursive:true});await writeFile(out,JSON.stringify(report,null,2)+'\n');console.log(`Build 198 contract: PASS (${patterns.length}/${patterns.length})`);
