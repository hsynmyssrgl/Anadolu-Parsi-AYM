import { execFileSync } from 'node:child_process';
import { mkdir, readFile, writeFile } from 'node:fs/promises';
import { dirname, join, resolve } from 'node:path';
import { pathToFileURL } from 'node:url';
const args=process.argv.slice(2),option=(name,fallback)=>{const index=args.indexOf(name);return index<0?fallback:args[index+1];},reportPath=resolve(option('--report','artifacts/validation/build157-renderer-bridge-syntax.json'));
const ts=(await import(pathToFileURL(join(execFileSync('npm',['root','-g'],{encoding:'utf8'}).trim(),'typescript','lib','typescript.js')).href)).default;
const files=['packages/domain/src/app-data.ts','apps/desktop/src/main/family-mutation-revision-service.ts','apps/desktop/src/main/data-store.ts','apps/desktop/src/renderer/App.tsx','apps/desktop/src/renderer/global.d.ts','apps/desktop/src/main/preload.ts','apps/desktop/src/main/main.ts'];const results=[],failures=[];
for(const path of files){const source=await readFile(path,'utf8'),sourceFile=ts.createSourceFile(path,source,ts.ScriptTarget.ES2024,true,path.endsWith('.tsx')?ts.ScriptKind.TSX:ts.ScriptKind.TS),diagnostics=(sourceFile.parseDiagnostics??[]).filter(item=>item.category===ts.DiagnosticCategory.Error).map(item=>ts.flattenDiagnosticMessageText(item.messageText,'\n'));results.push({path,status:diagnostics.length?'FAIL':'PASS',diagnostics});failures.push(...diagnostics.map(item=>`${path}: ${item}`));}
const main=await readFile('apps/desktop/src/main/main.ts','utf8'),preload=await readFile('apps/desktop/src/main/preload.ts','utf8'),mainChannels=[...main.matchAll(/registerIpcHandler\(\s*['"]([^'"]+)/g)].map(match=>match[1]),preloadChannels=[...preload.matchAll(/(?:ipcRenderer\.)?\binvoke(?:<[^>]+>)?\(\s*['"]([^'"]+)/g)].map(match=>match[1]),missingInPreload=[...new Set(mainChannels)].filter(channel=>!preloadChannels.includes(channel)),missingInMain=[...new Set(preloadChannels)].filter(channel=>!mainChannels.includes(channel));if(new Set(mainChannels).size!==new Set(preloadChannels).size)failures.push(`IPC unique channel count mismatch: ${new Set(mainChannels).size}/${new Set(preloadChannels).size}`);failures.push(...missingInPreload.map(channel=>`Main IPC missing in preload: ${channel}`),...missingInMain.map(channel=>`Preload IPC missing in main: ${channel}`));
const report={schemaVersion:1,product:'Anadolu Parsı Aile Yaşam Merkezi',featureBuild:157,applicationVersion:'29.07.2026.157',packageVersion:'29.7.2026-157',stage:'Bronze RC2 Active Development',typeScriptVersion:ts.version,fileCount:files.length,channelParity:{status:missingInPreload.length||missingInMain.length||new Set(mainChannels).size!==new Set(preloadChannels).size?'FAIL':'PASS',mainChannelCount:mainChannels.length,preloadChannelCount:preloadChannels.length,missingInPreload,missingInMain},status:failures.length?'FAIL':'PASS',results,failures,generatedAt:new Date().toISOString()};await mkdir(dirname(reportPath),{recursive:true});await writeFile(reportPath,`${JSON.stringify(report,null,2)}\n`);console.log(`Build 157 mutation syntax/parity: ${report.status} — ${files.length}/${files.length} files, ${mainChannels.length}/${preloadChannels.length} IPC channels`);if(failures.length)process.exitCode=1;
