import assert from 'node:assert/strict';
import {readFile,mkdir,writeFile} from 'node:fs/promises';
import {dirname,resolve} from 'node:path';
import {DatabaseSync} from 'node:sqlite';
const out=resolve(process.argv[2]??'artifacts/validation/build200-clean-rewrite-propagation-status-sqlite-runtime.json');
const src=await readFile('packages/database/src/family-database-migrations.ts','utf8');
const m199=/const cleanBackupRewritePropagationOutcomeIntegritySql = `([\s\S]*?)`;/m.exec(src);
const m200=/const cleanBackupRewritePropagationOutcomeStatusIntegritySql = `([\s\S]*?)`;/m.exec(src);
if(!m199||!m200) throw new Error('Build 199/200 migration SQL not found');
const base=`CREATE TABLE database_metadata(key TEXT PRIMARY KEY,value TEXT NOT NULL,updated_at TEXT NOT NULL);INSERT INTO database_metadata VALUES('schema_generation','BASE','2026-07-31T00:00:00.000Z');CREATE TABLE backup_propagation_runs(id TEXT PRIMARY KEY,status TEXT NOT NULL,pending_records INTEGER NOT NULL,target_count INTEGER NOT NULL,refreshed_targets INTEGER NOT NULL,quarantined_artifacts INTEGER NOT NULL,pending_remaining INTEGER NOT NULL,manual_backup_warning INTEGER NOT NULL,target_results TEXT NOT NULL,error TEXT,started_at TEXT NOT NULL,completed_at TEXT NOT NULL);CREATE TABLE backup_clean_rewrite_runs(id TEXT PRIMARY KEY,trigger TEXT NOT NULL,status TEXT NOT NULL,retention_cutoff TEXT NOT NULL,due_records INTEGER NOT NULL,enabled_targets INTEGER NOT NULL,propagation_run_id TEXT,next_attempt_at TEXT,error TEXT,started_at TEXT NOT NULL,completed_at TEXT,updated_at TEXT NOT NULL);`;
const propagationInsert=`INSERT INTO backup_propagation_runs VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`;
const cleanInsert=`INSERT INTO backup_clean_rewrite_runs VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`;
const make=(with200)=>{const db=new DatabaseSync(':memory:');db.exec(base);db.exec(m199[1]);if(with200)db.exec(m200[1]);for(const [id,status] of [['ps','success'],['pp','partial'],['pf','failed'],['pa','attention']])db.prepare(propagationInsert).run(id,status,1,1,1,0,0,1,'[]',null,'2026-07-31T00:00:00.000Z','2026-07-31T00:01:00.000Z');return db;};
const checks=[];const check=(label,fn)=>{fn();checks.push(label)};
let old=make(false);
check('build199 baseline permits success linked to failed propagation',()=>assert.equal(Number(old.prepare(cleanInsert).run('old','manual','success','2026-07-01T00:00:00.000Z',1,1,'pf',null,null,'2026-07-31T00:00:00.000Z','2026-07-31T00:02:00.000Z','2026-07-31T00:02:00.000Z').changes),1));old.close();
let db=make(true);
check('success rejects failed propagation',()=>assert.throws(()=>db.prepare(cleanInsert).run('s-f','manual','success','2026-07-01T00:00:00.000Z',1,1,'pf',null,null,'2026-07-31T00:00:00.000Z','2026-07-31T00:02:00.000Z','2026-07-31T00:02:00.000Z'),/requires successful propagation/));
check('success rejects attention propagation',()=>assert.throws(()=>db.prepare(cleanInsert).run('s-a','manual','success','2026-07-01T00:00:00.000Z',1,1,'pa',null,null,'2026-07-31T00:00:00.000Z','2026-07-31T00:02:00.000Z','2026-07-31T00:02:00.000Z'),/requires successful propagation/));
check('partial rejects success propagation',()=>assert.throws(()=>db.prepare(cleanInsert).run('p-s','manual','partial','2026-07-01T00:00:00.000Z',1,1,'ps','2026-08-01T00:00:00.000Z','partial','2026-07-31T00:00:00.000Z','2026-07-31T00:02:00.000Z','2026-07-31T00:02:00.000Z'),/requires partial propagation/));
check('partial rejects failed propagation',()=>assert.throws(()=>db.prepare(cleanInsert).run('p-f','manual','partial','2026-07-01T00:00:00.000Z',1,1,'pf','2026-08-01T00:00:00.000Z','partial','2026-07-31T00:00:00.000Z','2026-07-31T00:02:00.000Z','2026-07-31T00:02:00.000Z'),/requires partial propagation/));
check('success accepts success propagation',()=>assert.equal(Number(db.prepare(cleanInsert).run('s-s','manual','success','2026-07-01T00:00:00.000Z',1,1,'ps',null,null,'2026-07-31T00:00:00.000Z','2026-07-31T00:02:00.000Z','2026-07-31T00:02:00.000Z').changes),1));
check('partial accepts partial propagation',()=>assert.equal(Number(db.prepare(cleanInsert).run('p-p','manual','partial','2026-07-01T00:00:00.000Z',1,1,'pp','2026-08-01T00:00:00.000Z','partial','2026-07-31T00:00:00.000Z','2026-07-31T00:02:00.000Z','2026-07-31T00:02:00.000Z').changes),1));
check('update trigger rejects status mismatch',()=>assert.throws(()=>db.prepare(`UPDATE backup_clean_rewrite_runs SET propagation_run_id='pf' WHERE id='s-s'`).run(),/requires successful propagation/));
check('migration 44 registered',()=>assert.match(src,/createMigrationDefinition\(44, 'clean_backup_rewrite_propagation_status_integrity'/));
check('insert trigger exists',()=>assert.equal(Number(db.prepare(`SELECT COUNT(*) n FROM sqlite_master WHERE type='trigger' AND name='trg_backup_clean_rewrite_runs_propagation_status_insert'`).get().n),1));
check('update trigger exists',()=>assert.equal(Number(db.prepare(`SELECT COUNT(*) n FROM sqlite_master WHERE type='trigger' AND name='trg_backup_clean_rewrite_runs_propagation_status_update'`).get().n),1));
db.close();
const report={schemaVersion:1,product:'Anadolu Parsı Aile Yaşam Merkezi',featureBuild:200,status:'PASS',checks:checks.length,checkLabels:checks,generatedAt:new Date().toISOString()};await mkdir(dirname(out),{recursive:true});await writeFile(out,JSON.stringify(report,null,2)+'\n');console.log(`Build 200 propagation status SQLite: PASS (${checks.length}/${checks.length})`);
