import assert from 'node:assert/strict';
import { DatabaseSync } from 'node:sqlite';
import { mkdir, readFile, writeFile } from 'node:fs/promises';
import { dirname, resolve } from 'node:path';
const out=resolve(process.argv[2]??'artifacts/validation/build187-clean-rewrite-recovery-chronology-sqlite-runtime.json');
const source=await readFile('packages/database/src/family-database-migrations.ts','utf8');
const linked=/const cleanBackupRewriteLinkedChronologySql = `([\s\S]*?)`;\n/.exec(source);if(!linked)throw new Error('Build 186 linked chronology SQL not found.');
const recovery=/const cleanBackupRewriteRecoveryChronologySql = `([\s\S]*?)`;\n/.exec(source);if(!recovery)throw new Error('Build 187 recovery chronology SQL not found.');
const db=new DatabaseSync(':memory:');
db.exec(`PRAGMA foreign_keys=ON;CREATE TABLE database_metadata(key TEXT PRIMARY KEY,value TEXT NOT NULL,updated_at TEXT NOT NULL);INSERT INTO database_metadata VALUES('schema_generation','test','2026-07-30T00:00:00.000Z');CREATE TABLE backup_propagation_runs(id TEXT PRIMARY KEY,status TEXT,pending_records INTEGER,target_count INTEGER,refreshed_targets INTEGER,quarantined_artifacts INTEGER,pending_remaining INTEGER,manual_backup_warning INTEGER,target_results TEXT,error TEXT,started_at TEXT,completed_at TEXT);CREATE TABLE backup_clean_rewrite_policy(id TEXT PRIMARY KEY,enabled INTEGER NOT NULL,retention_days INTEGER NOT NULL,manual_failure_backoff_minutes INTEGER NOT NULL,automatic_failure_backoff_minutes INTEGER NOT NULL,high_load_defer_minutes INTEGER NOT NULL,state TEXT NOT NULL,consecutive_failures INTEGER NOT NULL,last_outcome TEXT NOT NULL,last_trigger TEXT,last_attempt_at TEXT,last_success_at TEXT,next_attempt_at TEXT,last_error TEXT,in_progress_run_id TEXT,in_progress_started_at TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL);CREATE TABLE backup_clean_rewrite_runs(id TEXT PRIMARY KEY,trigger TEXT NOT NULL,status TEXT NOT NULL,retention_cutoff TEXT NOT NULL,due_records INTEGER NOT NULL,enabled_targets INTEGER NOT NULL,propagation_run_id TEXT,next_attempt_at TEXT,error TEXT,started_at TEXT NOT NULL,completed_at TEXT,updated_at TEXT NOT NULL);`);
db.exec(linked[1]);db.exec(recovery[1]);
const checks=[];const check=(label,fn)=>{fn();checks.push(label);};const throws=(label,fn,pattern)=>check(label,()=>assert.throws(fn,pattern));
const insertPolicy=db.prepare(`INSERT INTO backup_clean_rewrite_policy VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`);
insertPolicy.run('default',1,30,60,360,30,'idle',0,'never',null,null,null,null,null,null,null,'2026-07-01T00:00:00.000Z','2026-07-30T12:00:00.000Z');
check('valid idle policy insert persists',()=>assert.equal(db.prepare(`SELECT state FROM backup_clean_rewrite_policy`).get().state,'idle'));
db.prepare(`UPDATE backup_clean_rewrite_policy SET state='running',last_trigger='automatic',last_attempt_at=?,next_attempt_at=NULL,in_progress_run_id='run-187',in_progress_started_at=?,updated_at=?`).run('2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z');
check('valid running claim persists',()=>assert.equal(db.prepare(`SELECT in_progress_run_id FROM backup_clean_rewrite_policy`).get().in_progress_run_id,'run-187'));
throws('running policy with stale next attempt is denied',()=>db.prepare(`UPDATE backup_clean_rewrite_policy SET next_attempt_at='2026-07-30T13:00:00.000Z'`).run(),/cannot retain next attempt/);
throws('running policy without owner is denied',()=>db.prepare(`UPDATE backup_clean_rewrite_policy SET in_progress_run_id=NULL`).run(),/requires owner and start/);
throws('non-running policy retaining owner is denied',()=>db.prepare(`UPDATE backup_clean_rewrite_policy SET state='backoff',next_attempt_at='2026-07-30T18:00:00.000Z'`).run(),/cannot retain owner/);

db.prepare(`INSERT INTO backup_clean_rewrite_runs VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`).run('run-187','automatic','running','2026-06-30T12:00:00.000Z',1,1,null,null,null,'2026-07-30T12:00:00.000Z',null,'2026-07-30T12:00:00.000Z');
check('valid running ledger persists',()=>assert.equal(db.prepare(`SELECT status FROM backup_clean_rewrite_runs`).get().status,'running'));
db.prepare(`UPDATE backup_clean_rewrite_runs SET status='interrupted',next_attempt_at='2026-07-30T18:00:00.000Z',error='interrupted',completed_at='2026-07-30T12:00:00.000Z',updated_at='2026-07-30T12:00:00.000Z' WHERE id='run-187'`).run();
check('clock-floor interrupted ledger persists',()=>assert.equal(db.prepare(`SELECT completed_at FROM backup_clean_rewrite_runs WHERE id='run-187'`).get().completed_at,'2026-07-30T12:00:00.000Z'));
db.prepare(`UPDATE backup_clean_rewrite_policy SET state='backoff',last_outcome='failed',consecutive_failures=1,next_attempt_at='2026-07-30T18:00:00.000Z',last_error='interrupted',in_progress_run_id=NULL,in_progress_started_at=NULL,updated_at='2026-07-30T12:00:00.000Z'`).run();
check('clock-floor recovery policy releases owner',()=>assert.equal(db.prepare(`SELECT in_progress_run_id FROM backup_clean_rewrite_policy`).get().in_progress_run_id,null));
check('clock-floor recovery policy keeps six-hour backoff',()=>assert.equal(db.prepare(`SELECT next_attempt_at FROM backup_clean_rewrite_policy`).get().next_attempt_at,'2026-07-30T18:00:00.000Z'));
throws('backoff policy without next attempt is denied',()=>db.prepare(`UPDATE backup_clean_rewrite_policy SET next_attempt_at=NULL`).run(),/requires next attempt/);
throws('invalid policy next attempt is denied',()=>db.prepare(`UPDATE backup_clean_rewrite_policy SET next_attempt_at='invalid'`).run(),/next attempt is invalid/);
throws('policy next attempt before update is denied',()=>db.prepare(`UPDATE backup_clean_rewrite_policy SET next_attempt_at='2026-07-30T11:59:59.000Z'`).run(),/precedes update/);

const insertRun=db.prepare(`INSERT INTO backup_clean_rewrite_runs VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`);
throws('interrupted run without next attempt is denied',()=>insertRun.run('no-next','automatic','interrupted','2026-06-30T00:00:00.000Z',1,1,null,null,'x','2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z'),/requires next attempt/);
throws('next attempt before completion is denied',()=>insertRun.run('early-next','automatic','failed','2026-06-30T00:00:00.000Z',1,1,null,'2026-07-30T11:59:59.000Z','x','2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z'),/precedes completion/);
throws('successful run with next attempt is denied',()=>insertRun.run('success-next','automatic','success','2026-06-30T00:00:00.000Z',1,1,null,'2026-07-30T18:00:00.000Z',null,'2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z'),/cannot retain next attempt/);
throws('running run with completion is denied',()=>insertRun.run('running-complete','automatic','running','2026-06-30T00:00:00.000Z',1,1,null,null,null,'2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z','2026-07-30T12:00:00.000Z'),/cannot be completed/);
throws('completed run without completion time is denied',()=>insertRun.run('failed-no-complete','automatic','failed','2026-06-30T00:00:00.000Z',1,1,null,'2026-07-30T18:00:00.000Z','x','2026-07-30T12:00:00.000Z',null,'2026-07-30T12:00:00.000Z'),/requires completion time/);
check('policy insert trigger exists',()=>assert.ok(db.prepare(`SELECT name FROM sqlite_master WHERE type='trigger' AND name='trg_backup_clean_rewrite_policy_state_insert'`).get()));
check('policy update trigger exists',()=>assert.ok(db.prepare(`SELECT name FROM sqlite_master WHERE type='trigger' AND name='trg_backup_clean_rewrite_policy_state_update'`).get()));
check('run retry insert trigger exists',()=>assert.ok(db.prepare(`SELECT name FROM sqlite_master WHERE type='trigger' AND name='trg_backup_clean_rewrite_runs_retry_insert'`).get()));
check('run retry update trigger exists',()=>assert.ok(db.prepare(`SELECT name FROM sqlite_master WHERE type='trigger' AND name='trg_backup_clean_rewrite_runs_retry_update'`).get()));
check('schema generation advanced',()=>assert.equal(db.prepare(`SELECT value FROM database_metadata WHERE key='schema_generation'`).get().value,'REVISION-187-CLEAN-BACKUP-RECOVERY-CHRONOLOGY'));
assert.equal(checks.length,22);
const report={schemaVersion:1,product:'Anadolu Parsı Aile Yaşam Merkezi',featureBuild:187,stage:'Bronze RC2 Active Development',scope:'Real SQLite restart recovery and retry chronology triggers',status:'PASS',checks:checks.length,checkLabels:checks,nodeSqlite:'node:sqlite DatabaseSync',generatedAt:new Date().toISOString()};await mkdir(dirname(out),{recursive:true});await writeFile(out,`${JSON.stringify(report,null,2)}\n`);db.close();console.log(`Build 187 clean rewrite recovery chronology SQLite runtime: PASS (${checks.length}/${checks.length})`);
