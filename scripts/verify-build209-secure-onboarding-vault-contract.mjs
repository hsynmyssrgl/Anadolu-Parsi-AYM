import { readFile, writeFile, mkdir, stat } from 'node:fs/promises';
import { createHash } from 'node:crypto';
const failures=[];let checks=0;const check=(c,m)=>{checks++;if(!c)failures.push(m)};
const text=p=>readFile(p,'utf8');const json=async p=>JSON.parse(await text(p));const exists=async p=>{try{await stat(p);return true}catch{return false}};
const ledger=await json('config/master-build-ledger.json');const constitution=await json('config/project-constitution.json');const policy=await json('config/master-build-ledger-policy.json');const canonicalRules=await json('config/canonical-rule-registry.json');
const rs=ledger.projectRules.versions.find((entry)=>entry.version==='PROJECT-RULES-2026-08-01-V4');check(Boolean(rs),'historical V4 rule set missing');
check(rs.version==='PROJECT-RULES-2026-08-01-V4',`rule version=${rs.version}`);check(rs.effectiveBuild===209,`effectiveBuild=${rs.effectiveBuild}`);check(rs.rules.length===170,`rule count=${rs.rules.length}`);check(rs.rules.at(-1)?.id==='PR-170','last rule must be PR-170');
const canonicalize=v=>Array.isArray(v)?`[${v.map(canonicalize).join(',')}]`:v&&typeof v==='object'?`{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${canonicalize(v[k])}`).join(',')}}`:JSON.stringify(v);
const expected=createHash('sha256').update(canonicalize({version:rs.version,effectiveBuild:rs.effectiveBuild,rules:rs.rules})).digest('hex');check(rs.sha256===expected,`rule SHA mismatch=${rs.sha256}`);check(rs.sha256==='6259d2c757caf865aedfe99a7bcea0a1a333551415b0912a856ac571876274f9','V4 expected SHA mismatch');
check(/^PPT-PROJECT-CONSTITUTION-V\d+$/.test(constitution.id) && constitution.schemaVersion>=3,'current constitution must preserve the governed security baseline');check(/^PPT-BUILD-LEDGER-CONTINUITY-V\d+$/.test(policy.policyId) && policy.effectiveBuild>=209,'current ledger policy must preserve the Build209 governance baseline');
check(constitution.canonicalRuleRegistry==='config/canonical-rule-registry.json','current constitution canonical rule registry binding missing');check(constitution.canonicalRulesSha256===canonicalRules.rulesSha256,'current constitution canonical rule hash mismatch');
for(const id of ['PR-141','PR-142','PR-145','PR-170'])check(canonicalRules.rules.some((rule)=>rule.id===id&&rule.state==='ACTIVE'),`active onboarding rule missing=${id}`);
check(constitution.preAuthenticationUserDataAccessForbidden===true,'pre-auth data gate disabled');check(constitution.persistentUserDataEncryptionRequired===true,'persistent encryption disabled');
const vault=await text('apps/desktop/src/main/user-data-vault.ts');
for(const token of ["createCipheriv('aes-256-gcm'","scryptSync(password","maxmem: 64 * 1024 * 1024","this.options.protector.protect","this.options.protector.unprotect","checkpoint(databaseBytes: Buffer)","this.discardSession()"]){check(vault.includes(token),`vault token missing=${token}`)}
const main=await text('apps/desktop/src/main/main.ts');
check(!main.includes('legacyProductName'),'automatic legacy product source transfer still present');check(!main.includes('cpSync(legacyUserDataPath'),'legacy user data copy still present');
check(main.includes("registerIpcHandler('auth:getState', () => dataStore ? dataStore.getAuthState() : lockedAuthState())"),'locked auth state gate missing');
check(main.includes("if (!session || !vault().isUnlocked()) throw new Error('Kullanıcı veri kasası kilitli.')"),'store does not require unlocked vault');check(main.includes('databaseConnection: session.database'),'current store is not composed over authenticated memory session');check(main.includes("securityEventReceiptPath: join(dirname(databasePath), 'security-event-receipts.json')")||main.includes("securityEventReceiptPath: join(dirname(databasePath), 'security-event-receipts.pptdiag')"),'security receipts are not session-local/protected');check(main.includes("migrationBackupDirectory: join(dirname(databasePath), 'migration-backups')"),'migration backups are not session-local');
check(main.includes("for(const provider of ['apple','google','microsoft'] as const)"),'Apple/Google/Microsoft provider vocabulary incomplete');
check(main.includes("if(provider==='apple'||clientAuthenticationMode!=='public_pkce'"),'unsupported Apple/private-client provider must remain fail-closed');
check(main.includes('secureOidcNetworkAdapter.networkReadyProviderRegistrations()'),'secure network-ready provider registry missing');
check(main.includes("oidcDeepLinkProtocolRegistered?oidcFederatedIdentity?.listVisibleConfiguredProviders()??[]:[]")&&main.includes('productionReady:false'),'provider visibility/live-readiness truth gate missing');
check(main.includes('openArchiveInSecurePreview'),'secure archive preview missing');check(!/shell\.openPath\s*\(/.test(main),'external decrypted archive open is still reachable');
check(main.includes('sealUserDataSession()'),'vault reseal flow missing');check(main.includes('startVaultSessionGuard()'),'session expiry guard missing');
const renderer=await text('apps/desktop/src/renderer/App.tsx');
for(const token of ['FirstRunIntroduction','startFirstRunNarration','firstRunNarrationContent','persistFirstRunIntroductionComplete','audioMuted={accessibility.audioMuted}','onAudioMutedChange','speechSynthesis','playParsBrandSound','FirstRunSecuritySetup','beginTwoFactorSetup','recoveryCodes','getExternalIdentityProviders','ppt-replay-intro'])check(renderer.includes(token),`renderer onboarding token missing=${token}`);
check(/auth\.authenticated&&sessionOverlay\s*\?\s*sessionOverlay\s*:\s*<FirstRunIntroduction/u.test(renderer),'locked session must suppress hidden onboarding narration');
check(renderer.indexOf('if(loading)return <main className="first-run-shell">')<renderer.indexOf('if(!firstRunIntroCompleted)'),'neutral secure startup must precede the interactive introduction');
const localization=await text('apps/desktop/src/renderer/localization.tsx');
check(localization.includes("'shell.loadingBody':'Kimlik durumu doğrulanmadan aile verileri ve normal uygulama ekranı açılmaz.'"),'pre-auth neutral loading truth missing');
check(renderer.includes("auth.authenticated && (!auth.twoFactorEnabled||auth.trustedDevice!==true)"),'first-run security completion gate missing');
const accessibility=await text('apps/desktop/src/renderer/accessibility.ts');
for(const token of ['FIRST_RUN_INTRO_STORAGE_KEY','BRAND_AUDIO_DISABLED_STORAGE_KEY','readBootstrapPreference','writeBootstrapPreference','startFirstRunNarration','utterance.lang = narration.locale',"input.onStatus('unavailable')"]){check(accessibility.includes(token),`safe onboarding controller token missing=${token}`)}
const accessibilityTest=await text('apps/desktop/tests/accessibility-preference-center.test.ts');
for(const token of ['keeps the introduction visible and never crashes when bootstrap storage is denied','starts narration immediately after an explicit unmute','falls back to the visible caption when speech is unavailable or fails'])check(accessibilityTest.includes(token),`onboarding regression test missing=${token}`);
const domain=await text('packages/domain/src/app-data.ts');check(domain.includes("'apple' | 'google' | 'microsoft'"),'domain identity provider union incomplete');
for(const p of ['docs/18_PROJECT_CONSTITUTION_V4.md','docs/decisions/DEC-099-secure-onboarding-and-user-data-vault.md','docs/adr/ADR-082-secure-onboarding-user-data-vault.md'])check(await exists(p),`required Build209 constitution artifact missing=${p}`);
const report={schemaVersion:2,build:209,currentConstitutionId:constitution.id,canonicalRulesSha256:canonicalRules.rulesSha256,ruleVersion:rs.version,ruleCount:rs.rules.length,ruleSha256:rs.sha256,checks,status:failures.length?'FAIL':'PASS',failures,generatedAt:new Date().toISOString()};
await mkdir('artifacts/validation',{recursive:true});await writeFile('artifacts/validation/build209-secure-onboarding-vault-contract.json',JSON.stringify(report,null,2)+'\n');
if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log(`Build 209 secure onboarding/vault contract: PASS (${checks} checks / ${rs.rules.length} rules).`);
