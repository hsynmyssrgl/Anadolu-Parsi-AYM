import { writeFile, mkdir } from 'node:fs/promises';import { execFileSync } from 'node:child_process';import { join } from 'node:path';
const files=['apps/desktop/src/renderer/App.tsx','apps/desktop/src/main/preload.ts','packages/domain/src/app-data.ts'];const failures=[];let checks=0;
for(const file of files){checks++;try{execFileSync(process.execPath,[join('node_modules','typescript','lib','tsc.js'),'--ignoreConfig',file,'--target','es2024','--module','esnext','--moduleResolution','bundler','--jsx','react-jsx','--types','node','--skipLibCheck','--noCheck','--noEmit'],{stdio:'pipe'})}catch(error){failures.push(`${file}: ${String(error.stdout||error.stderr||error.message||error)}`)}}
await mkdir('artifacts/validation',{recursive:true});await writeFile('artifacts/validation/build209-renderer-syntax.json',JSON.stringify({schemaVersion:1,build:209,checks,status:failures.length?'FAIL':'PASS',failures,generatedAt:new Date().toISOString()},null,2)+'\n');if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log(`Build 209 renderer/preload syntax: PASS (${checks} files).`);
