import { execFileSync } from 'node:child_process';import { mkdir, readFile, writeFile } from 'node:fs/promises';import { dirname, join, resolve } from 'node:path';import { pathToFileURL } from 'node:url';
const args=process.argv.slice(2),option=(n,f)=>{const i=args.indexOf(n);return i<0?f:args[i+1];},reportPath=resolve(option('--report','artifacts/validation/build168-ipc-adaptive-budget-maintenance-authority-syntax.json'));
const ts=(await import(pathToFileURL(join(execFileSync('npm',['root','-g'],{encoding:'utf8'}).trim(),'typescript','lib','typescript.js')).href)).default;
const files=['apps/desktop/src/main/ipc-adaptive-budget-maintenance-authority.ts','apps/desktop/src/main/ipc-adaptive-budget-maintenance-session.ts','apps/desktop/src/main/ipc-integration-policy.ts','apps/desktop/src/main/main.ts','apps/desktop/src/main/preload.ts','apps/desktop/src/renderer/App.tsx','apps/desktop/src/renderer/global.d.ts','packages/domain/src/app-data.ts'];const failures=[];
for(const file of files){const source=await readFile(file,'utf8');const kind=file.endsWith('.tsx')?ts.ScriptKind.TSX:file.endsWith('.d.ts')?ts.ScriptKind.TS:ts.ScriptKind.TS;const sf=ts.createSourceFile(file,source,ts.ScriptTarget.ES2022,true,kind);const errors=(sf.parseDiagnostics??[]).filter(x=>x.category===ts.DiagnosticCategory.Error);if(errors.length)failures.push(`${file}: ${errors.length} syntax diagnostic`);}
const report={schemaVersion:1,product:'Anadolu Parsı Aile Yaşam Merkezi',featureBuild:168,stage:'Bronze RC2 Active Development',status:failures.length?'FAIL':'PASS',fileCount:files.length,files,failures,generatedAt:new Date().toISOString()};await mkdir(dirname(reportPath),{recursive:true});await writeFile(reportPath,`${JSON.stringify(report,null,2)}\n`);if(failures.length){console.error(`Build 168 authority syntax: FAIL`);for(const f of failures)console.error(`- ${f}`);process.exitCode=1;}else console.log(`Build 168 authority syntax: PASS (${files.length}/${files.length}).`);
